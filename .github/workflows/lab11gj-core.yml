name: Labs 11G-J core integration

on:
  push:
    branches:
      - agent/lab-11g-j-arp-stp-links-acl

permissions:
  contents: write

jobs:
  integrate:
    if: ${{ github.event.head_commit.message != 'Integrate Labs 11G-J core models' }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/lab-11g-j-arp-stp-links-acl
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Integrate Builder core models
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import json

          # Ethernet: add persisted STP config, a bounded redundant VLAN-10 triangle, and STP-aware path truth.
          path=Path('src/builder/ethernet.ts')
          text=path.read_text()
          text="import { builderStpState, cloneBuilderStpConfig, createDefaultBuilderStpConfig, validateBuilderStpConfig, type BuilderStpConfig } from './stp.ts';\n\n"+text
          text=text.replace("export interface BuilderEthernetConfig {\n  vlans: BuilderEthernetVlan[];\n  devices: BuilderEthernetDevice[];\n  links: BuilderEthernetLink[];\n  layout: Record<string, BuilderEthernetPoint>;\n}","export interface BuilderEthernetConfig {\n  vlans: BuilderEthernetVlan[];\n  devices: BuilderEthernetDevice[];\n  links: BuilderEthernetLink[];\n  layout: Record<string, BuilderEthernetPoint>;\n  stp: BuilderStpConfig;\n}")
          text=text.replace("    layout: Object.fromEntries(Object.entries(config.layout).map(([id, point]) => [id, { ...point }])),\n  };","    layout: Object.fromEntries(Object.entries(config.layout).map(([id, point]) => [id, { ...point }])),\n    stp: cloneBuilderStpConfig(config.stp),\n  };")
          text=text.replace("export function createEmptyBuilderEthernetConfig(): BuilderEthernetConfig {\n  return { vlans: [], devices: [], links: [], layout: {} };\n}","export function createEmptyBuilderEthernetConfig(): BuilderEthernetConfig {\n  return { vlans: [], devices: [], links: [], layout: {}, stp: createDefaultBuilderStpConfig() };\n}")
          text=text.replace("      { id: 'lan-sw2', label: 'SW2', kind: 'switch', mac: '02:48:4f:00:02:02', interfaces: [] },\n      { id: 'lan-r1'","      { id: 'lan-sw2', label: 'SW2', kind: 'switch', mac: '02:48:4f:00:02:02', interfaces: [] },\n      { id: 'lan-sw3', label: 'SW3', kind: 'switch', mac: '02:48:4f:00:03:03', interfaces: [] },\n      { id: 'lan-r1'")
          text=text.replace("      { id: 'lan-sw1-sw2', a: 'lan-sw1', b: 'lan-sw2', mode: 'trunk', allowedVlans: [10, 20], failed: false },\n      { id: 'lan-b-sw2'","      { id: 'lan-sw1-sw2', a: 'lan-sw1', b: 'lan-sw2', mode: 'trunk', allowedVlans: [10, 20], failed: false },\n      { id: 'lan-sw1-sw3', a: 'lan-sw1', b: 'lan-sw3', mode: 'trunk', allowedVlans: [10], failed: false },\n      { id: 'lan-sw2-sw3', a: 'lan-sw2', b: 'lan-sw3', mode: 'trunk', allowedVlans: [10], failed: false },\n      { id: 'lan-b-sw2'")
          text=text.replace("      'lan-a': { x: 8, y: 18 }, 'lan-sw1': { x: 35, y: 35 }, 'lan-sw2': { x: 66, y: 35 },\n      'lan-b': { x: 92, y: 16 }, 'lan-c': { x: 92, y: 68 }, 'lan-r1': { x: 35, y: 82 },\n    },\n  };","      'lan-a': { x: 8, y: 18 }, 'lan-sw1': { x: 35, y: 35 }, 'lan-sw2': { x: 66, y: 35 }, 'lan-sw3': { x: 58, y: 68 },\n      'lan-b': { x: 92, y: 16 }, 'lan-c': { x: 92, y: 68 }, 'lan-r1': { x: 30, y: 82 },\n    },\n    stp: createDefaultBuilderStpConfig(),\n  };")
          text=text.replace("  if (!input || !Array.isArray(input.vlans) || !Array.isArray(input.devices) || !Array.isArray(input.links) || !input.layout || typeof input.layout !== 'object') {","  if (!input || !Array.isArray(input.vlans) || !Array.isArray(input.devices) || !Array.isArray(input.links) || !input.layout || typeof input.layout !== 'object') {")
          text=text.replace("  return cloneBuilderEthernetConfig(input);\n}","  const normalized = cloneBuilderEthernetConfig({ ...input, stp: cloneBuilderStpConfig(input.stp) });\n  normalized.stp = validateBuilderStpConfig(normalized, input.stp);\n  return normalized;\n}",1)
          text=text.replace("function linkCarriesVlan(link: BuilderEthernetLink, vlanId: number): boolean {\n  if (link.failed) return false;\n  return link.mode === 'access' ? link.accessVlan === vlanId : Boolean(link.allowedVlans?.includes(vlanId));\n}\n\nfunction deviceById", "function linkCarriesVlanRaw(link: BuilderEthernetLink, vlanId: number): boolean {\n  if (link.failed) return false;\n  return link.mode === 'access' ? link.accessVlan === vlanId : Boolean(link.allowedVlans?.includes(vlanId));\n}\n\nfunction linkCarriesVlan(config: BuilderEthernetConfig, link: BuilderEthernetLink, vlanId: number): boolean {\n  if (!linkCarriesVlanRaw(link, vlanId)) return false;\n  return !builderStpState(config, vlanId).blockedLinkIds.includes(link.id);\n}\n\nfunction deviceById")
          text=text.replace("  return config.links.filter((link) => linkCarriesVlan(link,vlanId) && (link.a===id || link.b===id))", "  return config.links.filter((link) => linkCarriesVlan(config,link,vlanId) && (link.a===id || link.b===id))")
          marker="function findL2Path(config: BuilderEthernetConfig, sourceId: string, destinationId: string, vlanId: number): { nodeIds: string[]; linkIds: string[] } | null {"
          text=text.replace(marker,"export function builderEthernetDeviceById(config: BuilderEthernetConfig, id: string): BuilderEthernetDevice | undefined { return deviceById(config,id); }\nexport function builderEthernetInterfaceFor(device: BuilderEthernetDevice | undefined, vlanId: number): BuilderEthernetInterface | undefined { return interfaceFor(device,vlanId); }\n\n"+marker)
          text=text.replace("function findL2Path(config: BuilderEthernetConfig, sourceId: string, destinationId: string, vlanId: number): { nodeIds: string[]; linkIds: string[] } | null {","export function builderEthernetPathForVlan(config: BuilderEthernetConfig, sourceId: string, destinationId: string, vlanId: number): { nodeIds: string[]; linkIds: string[] } | null {")
          text=text.replace("findL2Path(config,sourceId,destinationId,sourceVlan)","builderEthernetPathForVlan(config,sourceId,destinationId,sourceVlan)")
          text=text.replace("findL2Path(config,sourceId,router.id,sourceVlan)","builderEthernetPathForVlan(config,sourceId,router.id,sourceVlan)")
          text=text.replace("findL2Path(config,router.id,destinationId,destinationVlan)","builderEthernetPathForVlan(config,router.id,destinationId,destinationVlan)")
          loop_guard="  const fdb = new Map<string,BuilderEthernetFdbEntry>();\n\n  const sourceStp = builderStpState(config, sourceVlan);\n  if (!sourceStp.enabled && sourceStp.loopDetected) return fail(sourceId,destinationId,sourceVlan,destinationVlan,`STP is disabled while VLAN ${sourceVlan} contains a Layer-2 cycle. Broadcast/unknown-unicast forwarding is unsafe.`);\n  const destinationStp = builderStpState(config, destinationVlan);\n  if (!destinationStp.enabled && destinationStp.loopDetected) return fail(sourceId,destinationId,sourceVlan,destinationVlan,`STP is disabled while VLAN ${destinationVlan} contains a Layer-2 cycle. Broadcast/unknown-unicast forwarding is unsafe.`);"
          text=text.replace("  const fdb = new Map<string,BuilderEthernetFdbEntry>();",loop_guard)
          path.write_text(text)

          # Scenario v7: persist physical link profiles, ACL policy, and STP-bearing Ethernet config.
          path=Path('src/builder/scenario.ts'); text=path.read_text()
          text=text.replace("import { cloneBuilderEthernetConfig, createDefaultBuilderEthernetConfig, createEmptyBuilderEthernetConfig, validateBuilderEthernetConfig, type BuilderEthernetConfig } from './ethernet.ts';", "import { cloneBuilderEthernetConfig, createDefaultBuilderEthernetConfig, createEmptyBuilderEthernetConfig, validateBuilderEthernetConfig, type BuilderEthernetConfig } from './ethernet.ts';\nimport { cloneBuilderLinkProfiles, createDefaultBuilderLinkProfiles, validateBuilderLinkProfiles, type BuilderLinkProfiles } from './link-characteristics.ts';\nimport { cloneBuilderAclConfig, createDefaultBuilderAclConfig, validateBuilderAclConfig, type BuilderAclConfig } from './acl.ts';")
          text=text.replace("export type BuilderScenarioV6 = Omit<BuilderScenarioV5, 'version'> & { version: 6; ethernet: BuilderEthernetConfig };\nexport type BuilderScenario = BuilderScenarioV6;\n\nconst STORAGE_KEY = 'hopscotch.builder.scenarios.v6';\nconst LEGACY_STORAGE_KEYS = ['hopscotch.builder.scenarios.v5'", "export type BuilderScenarioV6 = Omit<BuilderScenarioV5, 'version'> & { version: 6; ethernet: BuilderEthernetConfig };\nexport type BuilderScenarioV7 = Omit<BuilderScenarioV6, 'version'> & { version: 7; linkProfiles: BuilderLinkProfiles; acl: BuilderAclConfig };\nexport type BuilderScenario = BuilderScenarioV7;\n\nconst STORAGE_KEY = 'hopscotch.builder.scenarios.v7';\nconst LEGACY_STORAGE_KEYS = ['hopscotch.builder.scenarios.v6', 'hopscotch.builder.scenarios.v5'")
          start=text.index("function validateV6(")
          end=text.index("function parseStoredList", start)
          replacement=r'''function validateV7(raw: Record<string, unknown>): BuilderScenarioV7 {
  if (raw.schema !== 'hopscotch.builder' || raw.version !== 7) throw new Error('Unsupported HOPSCOTCH Builder schema/version.');
  const graph = validateGraph(raw.graph);
  const sourceId = assertString(raw.sourceId, 'sourceId', 48);
  const destinationId = assertString(raw.destinationId, 'destinationId', 48);
  const ids = new Set(graph.nodes.map((node) => node.id));
  if (!ids.has(sourceId) || !ids.has(destinationId)) throw new Error('Source and destination must reference nodes that exist.');
  if (!isRecord(raw.addressing) || !isRecord(raw.addressing.segments) || !isRecord(raw.addressing.defaultGateways)) throw new Error('Builder schema v7 requires explicit L3 addressing.');
  const addressing = validateBuilderAddressing(graph, raw.addressing as unknown as BuilderAddressing);
  if (!isRecord(raw.routing) || !Array.isArray(raw.routing.staticRoutes)) throw new Error('Builder schema v7 requires explicit routing config.');
  if (!isRecord(raw.ethernet)) throw new Error('Builder schema v7 requires explicit Ethernet/VLAN configuration.');
  if (!isRecord(raw.linkProfiles)) throw new Error('Builder schema v7 requires routed link characteristics.');
  if (!isRecord(raw.acl)) throw new Error('Builder schema v7 requires ACL policy configuration.');
  return {
    schema: 'hopscotch.builder', version: 7, name: assertString(raw.name, 'Scenario name', 80), graph, addressing,
    routing: validateBuilderRoutingConfig(graph, addressing, raw.routing as unknown as BuilderRoutingConfig),
    ethernet: validateBuilderEthernetConfig(raw.ethernet as unknown as BuilderEthernetConfig),
    linkProfiles: validateBuilderLinkProfiles(graph, raw.linkProfiles as unknown as BuilderLinkProfiles),
    acl: validateBuilderAclConfig(graph, raw.acl as unknown as BuilderAclConfig), sourceId, destinationId,
    layout: validateLayout(raw.layout, graph), createdAt: assertTimestamp(raw.createdAt, 'createdAt'), updatedAt: assertTimestamp(raw.updatedAt, 'updatedAt'),
  };
}

function migrationDefaults(graph: BuilderGraph) { return { linkProfiles: createDefaultBuilderLinkProfiles(graph), acl: createDefaultBuilderAclConfig() }; }
function migrateV1(raw: Record<string, unknown>): BuilderScenarioV7 { const graph=validateGraph({nodes:raw.nodes,links:raw.links}); const addressing=createDefaultBuilderAddressing(graph); return validateV7({...raw,version:7,graph,addressing,routing:createDefaultBuilderRoutingConfig(),ethernet:createEmptyBuilderEthernetConfig(),...migrationDefaults(graph)}); }
function migrateV2(raw: Record<string, unknown>): BuilderScenarioV7 { const graph=validateGraph(raw.graph); const addressing=createDefaultBuilderAddressing(graph); return validateV7({...raw,version:7,graph,addressing,routing:createDefaultBuilderRoutingConfig(),ethernet:createEmptyBuilderEthernetConfig(),...migrationDefaults(graph)}); }
function migrateV3(raw: Record<string, unknown>): BuilderScenarioV7 { const graph=validateGraph(raw.graph); return validateV7({...raw,version:7,graph,routing:createDefaultBuilderRoutingConfig(),ethernet:createEmptyBuilderEthernetConfig(),...migrationDefaults(graph)}); }
function migrateV4(raw: Record<string, unknown>): BuilderScenarioV7 { const graph=validateGraph(raw.graph); return validateV7({...raw,version:7,ethernet:createEmptyBuilderEthernetConfig(),...migrationDefaults(graph)}); }
function migrateV5(raw: Record<string, unknown>): BuilderScenarioV7 { const graph=validateGraph(raw.graph); return validateV7({...raw,version:7,ethernet:createEmptyBuilderEthernetConfig(),...migrationDefaults(graph)}); }
function migrateV6(raw: Record<string, unknown>): BuilderScenarioV7 { const graph=validateGraph(raw.graph); return validateV7({...raw,version:7,...migrationDefaults(graph)}); }
function normalizeScenarioRecord(raw: Record<string, unknown>): BuilderScenarioV7 {
  if(raw.version===1)return migrateV1(raw); if(raw.version===2)return migrateV2(raw); if(raw.version===3)return migrateV3(raw); if(raw.version===4)return migrateV4(raw); if(raw.version===5)return migrateV5(raw); if(raw.version===6)return migrateV6(raw); return validateV7(raw);
}
export function deserializeBuilderScenario(text: string): BuilderScenarioV7 { let raw:unknown; try{raw=JSON.parse(text);}catch{throw new Error('Scenario file is not valid JSON.');} if(!isRecord(raw)||raw.schema!=='hopscotch.builder')throw new Error('File is not a HOPSCOTCH Builder scenario.'); return normalizeScenarioRecord(raw); }
export function serializeBuilderScenario(scenario: BuilderScenarioV7): string { return JSON.stringify(validateV7(scenario as unknown as Record<string, unknown>),null,2); }
export function createBuilderScenario(
  name:string, graph:BuilderGraph, sourceId:string, destinationId:string, layout:BuilderLayout,
  addressing:BuilderAddressing=createDefaultBuilderAddressing(graph), routing:BuilderRoutingConfig=createDefaultBuilderRoutingConfig(),
  existing?:BuilderScenarioV7, ethernet:BuilderEthernetConfig=createDefaultBuilderEthernetConfig(),
  linkProfiles:BuilderLinkProfiles=createDefaultBuilderLinkProfiles(graph), acl:BuilderAclConfig=createDefaultBuilderAclConfig(),
): BuilderScenarioV7 {
  const now=new Date().toISOString();
  return validateV7({schema:'hopscotch.builder',version:7,name,graph:cloneBuilderGraph(graph),addressing,routing:cloneBuilderRoutingConfig(routing),ethernet:cloneBuilderEthernetConfig(ethernet),linkProfiles:cloneBuilderLinkProfiles(linkProfiles),acl:cloneBuilderAclConfig(acl),sourceId,destinationId,layout:layoutForGraph(layout,graph),createdAt:existing?.createdAt??now,updatedAt:now});
}

'''
          text=text[:start]+replacement+text[end:]
          text=text.replace("BuilderScenarioV6[]","BuilderScenarioV7[]").replace("BuilderScenarioV6>","BuilderScenarioV7>").replace("BuilderScenarioV6):","BuilderScenarioV7):").replace("BuilderScenarioV6 {","BuilderScenarioV7 {")
          text=text.replace("const validated = validateV6(scenario as unknown as Record<string, unknown>);","const validated = validateV7(scenario as unknown as Record<string, unknown>);")
          text=text.replace("export function defaultBuilderScenario(): BuilderScenarioV7 {","export function defaultBuilderScenario(): BuilderScenarioV7 {")
          path.write_text(text)

          # Probes: consume routing + ACL truth and deterministic physical link characteristics.
          Path('src/builder/probes.ts').write_text(r'''import { interfacesForBuilderNode, type BuilderAddressing } from './addressing.ts';
import { traceBuilderForwarding, type BuilderForwardingTrace, type BuilderRoutingConfig } from './routing.ts';
import type { BuilderGraph } from './model.ts';
import { createDefaultBuilderLinkProfiles, deterministicBuilderPathDrop, builderPathCharacteristics, builderRoundTripCharacteristics, type BuilderLinkProfiles } from './link-characteristics.ts';
import { createDefaultBuilderAclConfig, traceBuilderPolicy, type BuilderAclConfig } from './acl.ts';

export type BuilderProbeKind = 'ping' | 'traceroute';
export type BuilderProbeStatus = 'echo-reply' | 'time-exceeded' | 'timeout' | 'unreachable';
export interface BuilderProbePacketSeed { id:string; label:string; sourceAddress:string; destinationAddress:string; sourceMac:string; destinationMac:string; ttl:number; }
export interface BuilderProbeAttempt {
  index:number; ttl:number; status:BuilderProbeStatus; responderNodeId:string|null; responderAddress:string|null;
  requestNodeIds:string[]; requestLinkIds:string[]; responseNodeIds:string[]; responseLinkIds:string[]; detail:string; packet:BuilderProbePacketSeed|null;
  simulatedRttMs:number|null; jitterMs:number; bottleneckMbps:number|null; pathMtuBytes:number|null; pathLossPercent:number; dropLinkId:string|null;
}
export interface BuilderProbeResult { id:string; sequence:number; kind:BuilderProbeKind; plane:'ROUTED IPV4'; sourceNodeId:string; destinationNodeId:string; sourceAddress:string|null; destinationAddress:string|null; success:boolean; attempts:BuilderProbeAttempt[]; summary:string; snapshotNote:string; }

const PROBE_PACKET_BYTES=84;
function nodeLabel(graph:BuilderGraph,nodeId:string):string{return graph.nodes.find((node)=>node.id===nodeId)?.label??nodeId.toUpperCase();}
function nodePath(trace:BuilderForwardingTrace):string[]{return[trace.sourceNodeId,...trace.hops.map((hop)=>hop.nextNodeId).filter((id):id is string=>Boolean(id))].filter((id,index,all)=>index===0||id!==all[index-1]);}
function linkPath(trace:BuilderForwardingTrace):string[]{return trace.hops.flatMap((hop)=>hop.linkId?[hop.linkId]:[]);}
function primaryAddress(addressing:BuilderAddressing,nodeId:string):string|null{return interfacesForBuilderNode(addressing,nodeId)[0]?.address??null;}
function inboundAddress(addressing:BuilderAddressing,nodeId:string,linkId:string|undefined):string|null{if(!linkId)return primaryAddress(addressing,nodeId);return addressing.segments[linkId]?.interfaces.find((entry)=>entry.nodeId===nodeId)?.address??primaryAddress(addressing,nodeId);}
function stableMac(nodeId:string,salt=''):string{const text=`${nodeId}:${salt}`;let hash=0x811c9dc5;for(let index=0;index<text.length;index+=1){hash^=text.charCodeAt(index);hash=Math.imul(hash,0x01000193)>>>0;}return[0x02,0x48,0x4f,(hash>>>16)&0xff,(hash>>>8)&0xff,hash&0xff].map((byte)=>byte.toString(16).padStart(2,'0')).join(':');}
function packetSeed(id:string,label:string,sourceNodeId:string,destinationNodeId:string,sourceAddress:string|null,destinationAddress:string|null,ttl:number):BuilderProbePacketSeed|null{if(!sourceAddress||!destinationAddress)return null;return{id,label,sourceAddress,destinationAddress,sourceMac:stableMac(sourceNodeId,'probe'),destinationMac:stableMac(destinationNodeId,'probe'),ttl};}
function metrics(profiles:BuilderLinkProfiles,requestLinkIds:string[],responseLinkIds:string[],hasResponse:boolean){const path=builderPathCharacteristics(profiles,[...requestLinkIds,...responseLinkIds]);const round=builderRoundTripCharacteristics(profiles,requestLinkIds,responseLinkIds);return{simulatedRttMs:hasResponse?Number(round.rttMs.toFixed(2)):null,jitterMs:Number(path.jitterMs.toFixed(2)),bottleneckMbps:path.bottleneckMbps,pathMtuBytes:path.pathMtuBytes,pathLossPercent:Number(path.lossPercent.toFixed(4))};}
function reverseTrace(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,fromNodeId:string,sourceNodeId:string):BuilderForwardingTrace{return traceBuilderForwarding(graph,addressing,routing,fromNodeId,sourceNodeId);}

function runPing(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,sourceNodeId:string,destinationNodeId:string,sequence:number,profiles:BuilderLinkProfiles,acl:BuilderAclConfig):BuilderProbeResult{
  const requestPolicy=traceBuilderPolicy(graph,addressing,routing,acl,sourceNodeId,destinationNodeId,'icmp'); const request=requestPolicy.forwarding;
  const sourceAddress=primaryAddress(addressing,sourceNodeId); const destinationAddress=request.destinationAddress??primaryAddress(addressing,destinationNodeId);
  let reply:BuilderForwardingTrace|null=null; let status:BuilderProbeStatus; let responderNodeId:string|null; let detail:string; let dropLinkId:string|null=null;
  const requestLinks=linkPath(request); const requestPhysical=builderPathCharacteristics(profiles,requestLinks);
  if(!request.reachable){status='unreachable';responderNodeId=request.failureNodeId;detail=request.failureReason??'Forward request could not be delivered.';}
  else if(!requestPolicy.permitted){status='unreachable';responderNodeId=requestPolicy.deniedAtRouterId;detail=`ACL / firewall policy denied the Echo Request: ${requestPolicy.explanation}`;}
  else if((requestPhysical.pathMtuBytes??PROBE_PACKET_BYTES)<PROBE_PACKET_BYTES){status='unreachable';responderNodeId=request.hops.find((hop)=>profiles[hop.linkId??'']?.mtuBytes<PROBE_PACKET_BYTES)?.nodeId??sourceNodeId;detail=`DF teaching probe is ${PROBE_PACKET_BYTES} bytes but path MTU is ${requestPhysical.pathMtuBytes}. Fragmentation is not fabricated.`;}
  else if((dropLinkId=deterministicBuilderPathDrop(profiles,requestLinks,`ping:${sequence}:request`))){status='timeout';responderNodeId=null;detail=`Echo Request was deterministically dropped on ${dropLinkId}; route and ACL policy were otherwise valid.`;}
  else{
    const replyPolicy=traceBuilderPolicy(graph,addressing,routing,acl,destinationNodeId,sourceNodeId,'icmp'); reply=replyPolicy.forwarding;
    const responseLinks=linkPath(reply);
    if(!reply.reachable){status='timeout';responderNodeId=destinationNodeId;detail=`Echo Request reached ${nodeLabel(graph,destinationNodeId)}, but the Echo Reply cannot return: ${reply.failureReason??'reverse path unavailable'}.`;}
    else if(!replyPolicy.permitted){status='timeout';responderNodeId=destinationNodeId;detail=`Echo Request arrived, but reverse ACL / firewall policy denied the Echo Reply: ${replyPolicy.explanation}`;}
    else if((dropLinkId=deterministicBuilderPathDrop(profiles,responseLinks,`ping:${sequence}:reply`))){status='timeout';responderNodeId=destinationNodeId;detail=`Echo Reply was deterministically dropped on ${dropLinkId}.`;}
    else{status='echo-reply';responderNodeId=destinationNodeId;detail='ICMP Echo Request and Echo Reply both pass forwarding, policy, MTU, and deterministic link-loss checks.';}
  }
  const responseLinks=reply?linkPath(reply):[]; const m=metrics(profiles,requestLinks,responseLinks,status==='echo-reply');
  const attempt:BuilderProbeAttempt={index:0,ttl:64,status,responderNodeId,responderAddress:responderNodeId?primaryAddress(addressing,responderNodeId):null,requestNodeIds:nodePath(request),requestLinkIds:requestLinks,responseNodeIds:reply?nodePath(reply):[],responseLinkIds:responseLinks,detail,packet:packetSeed(`probe-${sequence}-echo`,'ICMP ECHO REQUEST',sourceNodeId,destinationNodeId,sourceAddress,destinationAddress,64),...m,dropLinkId};
  return{id:`probe-${sequence}-ping`,sequence,kind:'ping',plane:'ROUTED IPV4',sourceNodeId,destinationNodeId,sourceAddress,destinationAddress,success:status==='echo-reply',attempts:[attempt],summary:status==='echo-reply'?`${nodeLabel(graph,destinationNodeId)} replied · simulated RTT ${m.simulatedRttMs} ms · path MTU ${m.pathMtuBytes}.`:status==='timeout'?'Request/reply forwarding exists only partially; policy or link behavior prevented a returning Echo Reply.':`${nodeLabel(graph,responderNodeId??request.failureNodeId??sourceNodeId)} stopped the Echo Request.`,snapshotNote:'Probe history is session-only. RTT comes from explicit link latency, never OSPF cost.'};
}

function runTraceroute(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,sourceNodeId:string,destinationNodeId:string,sequence:number,profiles:BuilderLinkProfiles,acl:BuilderAclConfig):BuilderProbeResult{
  const fullPolicy=traceBuilderPolicy(graph,addressing,routing,acl,sourceNodeId,destinationNodeId,'icmp'); const forward=fullPolicy.forwarding;
  const sourceAddress=primaryAddress(addressing,sourceNodeId); const destinationAddress=forward.destinationAddress??primaryAddress(addressing,destinationNodeId);
  const nodes=nodePath(forward); const links=linkPath(forward); const attempts:BuilderProbeAttempt[]=[]; let ttl=1; let terminal=false;
  for(let nodeIndex=1;nodeIndex<nodes.length;nodeIndex+=1){
    const nodeId=nodes[nodeIndex]; const node=graph.nodes.find((candidate)=>candidate.id===nodeId); if(!node||node.kind!=='router')continue;
    const requestLinks=links.slice(0,nodeIndex); const packet=packetSeed(`probe-${sequence}-ttl-${ttl}`,`ICMP TRACE TTL ${ttl}`,sourceNodeId,destinationNodeId,sourceAddress,destinationAddress,ttl);
    const physical=builderPathCharacteristics(profiles,requestLinks);
    if(fullPolicy.deniedAtRouterId===nodeId){const m=metrics(profiles,requestLinks,[],false);attempts.push({index:attempts.length,ttl,status:'unreachable',responderNodeId:nodeId,responderAddress:inboundAddress(addressing,nodeId,links[nodeIndex-1]),requestNodeIds:nodes.slice(0,nodeIndex+1),requestLinkIds:requestLinks,responseNodeIds:[],responseLinkIds:[],detail:`${nodeLabel(graph,nodeId)} denies the TTL-${ttl} probe: ${fullPolicy.explanation}`,packet,...m,dropLinkId:null});terminal=true;break;}
    if((physical.pathMtuBytes??PROBE_PACKET_BYTES)<PROBE_PACKET_BYTES){const m=metrics(profiles,requestLinks,[],false);attempts.push({index:attempts.length,ttl,status:'unreachable',responderNodeId:nodeId,responderAddress:inboundAddress(addressing,nodeId,links[nodeIndex-1]),requestNodeIds:nodes.slice(0,nodeIndex+1),requestLinkIds:requestLinks,responseNodeIds:[],responseLinkIds:[],detail:`DF traceroute probe exceeds path MTU ${physical.pathMtuBytes}.`,packet,...m,dropLinkId:null});terminal=true;break;}
    const requestDrop=deterministicBuilderPathDrop(profiles,requestLinks,`trace:${sequence}:${ttl}:request`);
    if(requestDrop){const m=metrics(profiles,requestLinks,[],false);attempts.push({index:attempts.length,ttl,status:'timeout',responderNodeId:null,responderAddress:null,requestNodeIds:nodes.slice(0,nodeIndex+1),requestLinkIds:requestLinks,responseNodeIds:[],responseLinkIds:[],detail:`TTL-${ttl} probe was dropped on ${requestDrop}.`,packet,...m,dropLinkId:requestDrop});ttl+=1;continue;}
    const responsePolicy=traceBuilderPolicy(graph,addressing,routing,acl,nodeId,sourceNodeId,'icmp'); const response=responsePolicy.forwarding; const responseLinks=linkPath(response); const responseDrop=response.reachable&&responsePolicy.permitted?deterministicBuilderPathDrop(profiles,responseLinks,`trace:${sequence}:${ttl}:reply`):null;
    const ok=response.reachable&&responsePolicy.permitted&&!responseDrop; const m=metrics(profiles,requestLinks,responseLinks,ok);
    attempts.push({index:attempts.length,ttl,status:ok?'time-exceeded':'timeout',responderNodeId:nodeId,responderAddress:inboundAddress(addressing,nodeId,links[nodeIndex-1]),requestNodeIds:nodes.slice(0,nodeIndex+1),requestLinkIds:requestLinks,responseNodeIds:ok?nodePath(response):[],responseLinkIds:ok?responseLinks:[],detail:ok?`${nodeLabel(graph,nodeId)} decrements TTL to zero and returns ICMP Time Exceeded · ${m.simulatedRttMs} ms simulated RTT.`:!response.reachable?`${nodeLabel(graph,nodeId)} expires TTL, but Time Exceeded has no reverse route.`:!responsePolicy.permitted?`Time Exceeded is denied on its reverse policy path: ${responsePolicy.explanation}`:`Time Exceeded was dropped on ${responseDrop}.`,packet,...m,dropLinkId:responseDrop}); ttl+=1;
  }
  if(!terminal){
    if(forward.reachable&&fullPolicy.permitted){const replyPolicy=traceBuilderPolicy(graph,addressing,routing,acl,destinationNodeId,sourceNodeId,'icmp');const reply=replyPolicy.forwarding;const responseLinks=linkPath(reply);const requestDrop=deterministicBuilderPathDrop(profiles,links,`trace:${sequence}:${ttl}:request`);const responseDrop=!requestDrop&&reply.reachable&&replyPolicy.permitted?deterministicBuilderPathDrop(profiles,responseLinks,`trace:${sequence}:${ttl}:reply`):null;const ok=!requestDrop&&reply.reachable&&replyPolicy.permitted&&!responseDrop;const m=metrics(profiles,links,responseLinks,ok);attempts.push({index:attempts.length,ttl,status:ok?'echo-reply':'timeout',responderNodeId:destinationNodeId,responderAddress:destinationAddress,requestNodeIds:nodes,requestLinkIds:links,responseNodeIds:ok?nodePath(reply):[],responseLinkIds:ok?responseLinks:[],detail:ok?`${nodeLabel(graph,destinationNodeId)} returns Echo Reply · ${m.simulatedRttMs} ms simulated RTT.`:requestDrop?`Final Echo probe was dropped on ${requestDrop}.`:!reply.reachable?'Final Echo Reply has no reverse route.':!replyPolicy.permitted?`Final Echo Reply is denied by reverse ACL policy: ${replyPolicy.explanation}`:`Final Echo Reply was dropped on ${responseDrop}.`,packet:packetSeed(`probe-${sequence}-ttl-${ttl}`,`ICMP TRACE TTL ${ttl}`,sourceNodeId,destinationNodeId,sourceAddress,destinationAddress,ttl),...m,dropLinkId:requestDrop??responseDrop});}
    else if(!forward.reachable){const m=metrics(profiles,links,[],false);attempts.push({index:attempts.length,ttl,status:'unreachable',responderNodeId:forward.failureNodeId,responderAddress:forward.failureNodeId?primaryAddress(addressing,forward.failureNodeId):null,requestNodeIds:nodes,requestLinkIds:links,responseNodeIds:[],responseLinkIds:[],detail:`${nodeLabel(graph,forward.failureNodeId??sourceNodeId)} cannot continue the probe: ${forward.failureReason??'no route'}.`,packet:packetSeed(`probe-${sequence}-ttl-${ttl}`,`ICMP TRACE TTL ${ttl}`,sourceNodeId,destinationNodeId,sourceAddress,destinationAddress,ttl),...m,dropLinkId:null});}
  }
  const success=attempts.at(-1)?.status==='echo-reply';
  return{id:`probe-${sequence}-traceroute`,sequence,kind:'traceroute',plane:'ROUTED IPV4',sourceNodeId,destinationNodeId,sourceAddress,destinationAddress,success,attempts,summary:success?`ICMP traceroute reached ${nodeLabel(graph,destinationNodeId)} after ${Math.max(0,attempts.length-1)} routed hop${attempts.length-1===1?'':'s'}.`:'Traceroute terminated without a returning destination Echo Reply.',snapshotNote:'TTL decrements only at routers. RTT/loss/MTU come from explicit link characteristics; ACL decisions remain separate from route truth.'};
}

export function runBuilderProbe(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,kind:BuilderProbeKind,sourceNodeId:string,destinationNodeId:string,sequence=1,profiles:BuilderLinkProfiles=createDefaultBuilderLinkProfiles(graph),acl:BuilderAclConfig=createDefaultBuilderAclConfig()):BuilderProbeResult{return kind==='ping'?runPing(graph,addressing,routing,sourceNodeId,destinationNodeId,sequence,profiles,acl):runTraceroute(graph,addressing,routing,sourceNodeId,destinationNodeId,sequence,profiles,acl);}
''')

          # Update package scripts.
          path=Path('package.json'); data=json.loads(path.read_text()); scripts=data['scripts']
          scripts['test:builder-arp-stp-contract']='node scripts/builder-arp-stp-contract-check.mjs'
          scripts['test:builder-link-policy-contract']='node scripts/builder-link-policy-contract-check.mjs'
          scripts['check']=scripts['check'].replace('npm run test:builder-ethernet-contract &&','npm run test:builder-ethernet-contract && npm run test:builder-arp-stp-contract && npm run test:builder-link-policy-contract &&')
          path.write_text(json.dumps(data,indent=2)+'\n')

          # Update existing Builder tests for schema v7 and the intentionally redundant VLAN-10 topology.
          for name in ['scripts/builder-addressing-contract-check.mjs','scripts/builder-ospf-contract-check.mjs','scripts/high-density-contract-check.mjs']:
            p=Path(name); t=p.read_text().replace('version,6','version,7').replace('version === 6','version === 7').replace('schema-v6','schema-v7').replace('schema v6','schema v7'); p.write_text(t)
          p=Path('scripts/builder-ethernet-contract-check.mjs'); t=p.read_text()
          t=t.replace("assert.equal(validateBuilderEthernetConfig(fabric).devices.filter((d)=>d.kind==='switch').length,2);","assert.equal(validateBuilderEthernetConfig(fabric).devices.filter((d)=>d.kind==='switch').length,3);")
          t=t.replace("assert.equal(runBuilderEthernetFlow(downTrunk,'lan-a','lan-b').success,false);","const stpFailover=runBuilderEthernetFlow(downTrunk,'lan-a','lan-b'); assert.equal(stpFailover.success,true); assert.deepEqual(stpFailover.segments[0].nodeIds,['lan-a','lan-sw1','lan-sw3','lan-sw2','lan-b']); assert.equal(runBuilderEthernetFlow(downTrunk,'lan-a','lan-c').success,false);")
          t=t.replace('assert.equal(scenario.version,6);','assert.equal(scenario.version,7);').replace('assert.equal(migrated.version,6);','assert.equal(migrated.version,7);')
          t=t.replace("const legacyV5={...scenario,version:5}; delete legacyV5.ethernet;","const legacyV5={...scenario,version:5}; delete legacyV5.ethernet; delete legacyV5.linkProfiles; delete legacyV5.acl;")
          p.write_text(t)

          # Roadmap slices.
          p=Path('docs/ROADMAP.md'); t=p.read_text()
          anchor='## Performance + rendering — ongoing'
          addition='''### 11G — ARP + Layer-2/Layer-3 resolution\n- [x] same-subnet endpoints ARP for the destination while off-subnet endpoints ARP for their configured gateway\n- [x] routed inter-VLAN delivery performs independent gateway-side and destination-side ARP resolution\n- [x] ARP Request broadcast and Reply unicast follow the current VLAN + STP forwarding topology\n- [x] session-only ARP cache produces explicit cache hits and can be cleared without mutating topology\n- [x] unresolved targets, blocked paths, and unsafe STP-disabled loops fail closed\n\n### 11H — STP / Layer-2 loop control\n- [x] deterministic root-bridge election from bridge priority + MAC + stable device ID\n- [x] per-VLAN root-path tree with explicit FORWARDING/BLOCKING switch segments\n- [x] redundant VLAN-10 switch triangle demonstrates blocked links without changing VLAN-20 trunk-isolation truth\n- [x] forwarding-link failure recomputes the tree and activates the alternate trunk\n- [x] STP-disabled Layer-2 cycles are surfaced as unsafe instead of silently terminating looping broadcasts\n\n### 11I — Routed link characteristics\n- [x] routing cost remains independent from latency, jitter, bandwidth, loss, MTU, and queue capacity\n- [x] deterministic link profiles persist per routed link and reconcile with topology edits\n- [x] Ping/Traceroute report simulated RTT, jitter, bottleneck bandwidth, path MTU, and aggregate loss from actual forwarding links\n- [x] deterministic replayable loss sampling; no random/non-reproducible packet outcomes\n- [x] DF teaching probes fail explicitly when packet size exceeds path MTU; fragmentation is not fabricated\n\n### 11J — ACL / firewall policy\n- [x] ordered per-router permit/deny rules with IPv4 prefixes, protocol, and optional TCP/UDP destination port\n- [x] first-match rule semantics with explicit default action\n- [x] route reachability and policy permission stay separate truth dimensions\n- [x] ICMP probes evaluate forward and reverse policy independently, including Time Exceeded replies\n- [x] ACL configuration persists while per-flow decisions remain derived\n\n'''
          if addition.splitlines()[0] not in t: t=t.replace(anchor,addition+anchor)
          p.write_text(t)
          PY

          npm ci
          npm run check
      - name: Commit core integration
        run: |
          rm .github/workflows/lab11gj-core.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Integrate Labs 11G-J core models"
          git push origin HEAD:agent/lab-11g-j-arp-stp-links-acl
