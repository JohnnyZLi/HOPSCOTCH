name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Apply Track K closeout once
        run: |
          if grep -q "applyCliMutation" src/NetworkBuilder.tsx; then
            echo "Track K closeout integration already applied."
          else
            python scripts/apply-track-k-closeout.py
          fi
          python scripts/fix-track-k-closeout.py
      - run: npm ci
      - run: npm run check
      - name: Commit generated Track K closeout
        run: |
          if git diff --quiet; then
            echo "No generated product changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- src/NetworkBuilder.tsx src/builder/cli-operations.ts scripts/builder-cli-contract-check.mjs docs/ROADMAP.md
          git commit -m "Integrate Track K closeout"
          git push origin HEAD:${{ github.head_ref }}
