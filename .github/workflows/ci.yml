name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/track-e-data-plane-realism
          fetch-depth: 0
      - name: Apply Track E integration once
        run: |
          if [ -f scripts/track-e-integrate.py ]; then
            python scripts/track-e-integrate.py
            rm scripts/track-e-integrate.py
            git show origin/main:.github/workflows/ci.yml > .github/workflows/ci.yml
            git config user.name "Johnny"
            git config user.email "92900710+JohnnyZLi@users.noreply.github.com"
            git add src/builder/data-plane.ts package.json scripts/track-e-integrate.py .github/workflows/ci.yml
            git commit -m "Track E: integrate fair scheduling and contract"
            git push origin HEAD:agent/track-e-data-plane-realism
          fi
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run check
      - name: Upload production build
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: hopscotch-dist
          path: dist
          if-no-files-found: error
          retention-days: 3
