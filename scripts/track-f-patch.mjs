import { readFileSync, writeFileSync, unlinkSync } from 'node:fs';

function patch(path, transform){const before=readFileSync(path,'utf8');const after=transform(before);if(after===before)throw new Error(`No changes applied to ${path}`);writeFileSync(path,after);}
function once(text,from,to,label){const i=text.indexOf(from);if(i<0)throw new Error(`Missing ${label}`);if(text.indexOf(from,i+1)>=0)throw new Error(`Ambiguous ${label}`);return text.slice(0,i)+to+text.slice(i+from.length);}

patch('src/builder/routing.ts',(source)=>{
  let s=source;
  s=once(s,"import type { BuilderGraph } from './model.ts';",`import type { BuilderGraph } from './model.ts';\nimport { builderBgpState, cloneBuilderBgpConfig } from './bgp.ts';\nimport { builderIsisRouteEntriesForRouter, builderIsisState, type BuilderIsisInjectedRoute } from './isis.ts';\nimport {\n  builderCanonicalEcmpKey,\n  builderEcmpProfile,\n  builderOspfEffectiveGraph,\n  builderOspfTimerCompatible,\n  builderPbrDecision,\n  builderPolicyPrefixContainsPrefix,\n  cloneBuilderRoutingPolicyConfig,\n  createDefaultBuilderRoutingPolicyConfig,\n  reconcileBuilderRoutingPolicyConfig,\n  validateBuilderRoutingPolicyConfig,\n  type BuilderRoutingPolicyConfig,\n  type BuilderRedistributionSource,\n} from './routing-policy.ts';`,'routing policy imports');
  s=once(s,"export type BuilderRouteSource = base.BuilderRouteSource;\nexport type BuilderForwardingHop = base.BuilderForwardingHop;\nexport type BuilderForwardingTrace = base.BuilderForwardingTrace;",`export type BuilderRouteSource = base.BuilderRouteSource | 'isis' | 'summary';\nexport interface BuilderForwardingHop extends Omit<base.BuilderForwardingHop,'routeSource'> {\n  routeSource: 'endpoint-local' | 'default-gateway' | BuilderRouteSource;\n  fibRouteSource?: BuilderRouteSource; fibMatchedPrefix?: string | null; fibNextHop?: string | null;\n  pbrRuleId?: string | null; pbrNextHop?: string | null; ecmpHashMode?: 'l3'|'l4'|'full';\n}\nexport interface BuilderForwardingTrace extends Omit<base.BuilderForwardingTrace,'hops'> { hops: BuilderForwardingHop[] }`,'route source types');
  s=once(s,"export interface BuilderRoutingConfig {\n  staticRoutes: BuilderStaticRoute[];\n  ospf: BuilderOspfConfig;\n  bgp: BuilderBgpConfig;\n}",`export interface BuilderRoutingConfig {\n  staticRoutes: BuilderStaticRoute[];\n  ospf: BuilderOspfConfig;\n  bgp: BuilderBgpConfig;\n  policy: BuilderRoutingPolicyConfig;\n}`,'routing config policy');
  s=once(s,"export interface BuilderRouteTableEntry extends Omit<base.BuilderRouteTableEntry, 'ospfRouteType'> {",`export interface BuilderRouteTableEntry extends Omit<base.BuilderRouteTableEntry, 'ospfRouteType' | 'source'> {\n  source: BuilderRouteSource;`,'route table source widening');
  s=once(s,"  ospfRedistributionId?: string | null;\n}",`  ospfRedistributionId?: string | null;\n  redistributedFrom?: BuilderRedistributionSource | 'summary';\n  redistributionRuleId?: string | null;\n  routeTag?: number;\n  summaryDiscard?: boolean;\n  summarySource?: BuilderRedistributionSource;\n  summaryId?: string | null;\n  isisLevel?: 'L1' | 'L2';\n  isisAreaId?: string;\n  isisOriginRouterId?: string;\n}`,'route table metadata');
  s=once(s,"export function createDefaultBuilderRoutingConfig(): BuilderRoutingConfig {\n  const result = base.createDefaultBuilderRoutingConfig();\n  return { ...result, ospf: { ...result.ospf, areaTypes: {}, redistributions: [] } };\n}",`export function createDefaultBuilderRoutingConfig(): BuilderRoutingConfig {\n  const result = base.createDefaultBuilderRoutingConfig();\n  return { ...result, ospf: { ...result.ospf, areaTypes: {}, redistributions: [] }, policy: createDefaultBuilderRoutingPolicyConfig() };\n}`,'default policy');
  s=once(s,"  return {\n    ...baseRouting,\n    ospf: {\n      ...baseRouting.ospf,\n      areaTypes: extensions.areaTypes,\n      redistributions: extensions.redistributions,\n    },\n  };",`  return {\n    ...baseRouting,\n    ospf: { ...baseRouting.ospf, areaTypes: extensions.areaTypes, redistributions: extensions.redistributions },\n    policy: validateBuilderRoutingPolicyConfig(graph,addressing,value.policy),\n  };`,'validate policy');
  s=once(s,"      redistributions: (value.ospf?.redistributions ?? []).map((entry) => ({ ...entry })),\n    },\n  };",`      redistributions: (value.ospf?.redistributions ?? []).map((entry) => ({ ...entry })),\n    },\n    policy: cloneBuilderRoutingPolicyConfig(value.policy),\n  };`,'clone policy');
  s=once(s,"  return validateBuilderRoutingConfig(graph, addressing, { ...reconciled, ospf: { ...reconciled.ospf, areaTypes, redistributions } });",`  return validateBuilderRoutingConfig(graph, addressing, { ...reconciled, ospf: { ...reconciled.ospf, areaTypes, redistributions }, policy: reconcileBuilderRoutingPolicyConfig(graph,addressing,current.policy) });`,'reconcile policy');
  s=once(s,"      redistributions: redistributions.map((entry) => ({ ...entry })),\n    },\n  });",`      redistributions: redistributions.map((entry) => ({ ...entry })),\n    },\n    policy: cloneBuilderRoutingPolicyConfig(source.policy),\n  });`,'preserve policy');
  s=s.replaceAll("{ ...result, ospf: { ...result.ospf, areaTypes: routing.ospf.areaTypes ?? {}, redistributions: routing.ospf.redistributions ?? [] } }","{ ...result, ospf: { ...result.ospf, areaTypes: routing.ospf.areaTypes ?? {}, redistributions: routing.ospf.redistributions ?? [] }, policy: cloneBuilderRoutingPolicyConfig(routing.policy) }");
  s=once(s,"export function builderOspfState(graph: BuilderGraph, addressing: BuilderAddressing, routing: BuilderRoutingConfig): BuilderOspfState {\n  return base.builderOspfState(graph, addressing, toBaseRouting(routing));\n}",`export function builderOspfState(graph: BuilderGraph, addressing: BuilderAddressing, routing: BuilderRoutingConfig): BuilderOspfState {\n  const effective=builderOspfEffectiveGraph(graph,routing.policy,routing.ospf.enabledRouterIds);\n  const state=base.builderOspfState(effective,addressing,toBaseRouting(routing));\n  return {...state,adjacencies:state.adjacencies.map((adj)=>{const physical=graph.links.find((link)=>link.id===adj.linkId);if(physical&&!physical.failed&&!builderOspfTimerCompatible(routing.policy,adj.aRouterId,adj.bRouterId,adj.linkId))return{...adj,state:'DOWN' as const,reason:'HELLO/DEAD TIMER MISMATCH'};return adj;})};\n}\n\nexport function setBuilderRoutingPolicyConfig(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,policy:BuilderRoutingPolicyConfig):BuilderRoutingConfig{\n  return validateBuilderRoutingConfig(graph,addressing,{...cloneBuilderRoutingConfig(routing),policy});\n}`,'effective ospf state');

  const insertPoint="function ospfExternalEntries(graph: BuilderGraph, addressing: BuilderAddressing, routing: BuilderRoutingConfig, routerId: string): BuilderRouteTableEntry[] {";
  const helpers=`function nativePolicyRoutes(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,routerId:string,source:BuilderRedistributionSource,ospfGraph:BuilderGraph):BuilderRouteTableEntry[]{\n  if(source==='connected'||source==='static'||source==='ospf')return (base.routeTableForBuilderRouter(ospfGraph,addressing,toBaseRouting(routing),routerId,ospfGraph) as BuilderRouteTableEntry[]).filter((entry)=>entry.source===source);\n  if(source==='bgp'){const state=builderBgpState(graph,addressing,routing.bgp);return state.bestRoutes.filter((route)=>route.routerId===routerId&&route.learnedVia!=='local').map((route)=>({id:\`policy-bgp:\${route.id}\`,routerId,prefix:route.prefix,prefixLength:Number(route.prefix.split('/')[1]),source:'bgp' as const,administrativeDistance:route.learnedVia==='ebgp'?20:200,metric:route.asPath.length*1000+route.med,nextHop:route.nextHopAddress,outgoingInterface:'—',linkId:'—',active:true,stateNote:'NATIVE BGP BEST · REDISTRIBUTION SOURCE'}));}\n  return builderIsisRouteEntriesForRouter(graph,addressing,routing.policy.isis,routerId).map((entry)=>({...entry,source:'isis' as const}));\n}\nfunction matchingNativePolicyRoutes(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,routerId:string,source:BuilderRedistributionSource,prefix:string,ospfGraph:BuilderGraph){return nativePolicyRoutes(graph,addressing,routing,routerId,source,ospfGraph).filter((entry)=>entry.active&&builderPolicyPrefixContainsPrefix(prefix,entry.prefix));}\nfunction summaryActive(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,summary:BuilderRoutingPolicyConfig['summaries'][number],ospfGraph:BuilderGraph){return matchingNativePolicyRoutes(graph,addressing,routing,summary.routerId,summary.source,summary.prefix,ospfGraph).some((entry)=>entry.prefix!==summary.prefix);}\nfunction effectiveBgpConfig(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,ospfGraph:BuilderGraph):BuilderBgpConfig{\n  const bgp=cloneBuilderBgpConfig(routing.bgp);for(const rule of routing.policy.redistributions.filter((entry)=>entry.enabled&&entry.target==='bgp'))for(const route of matchingNativePolicyRoutes(graph,addressing,routing,rule.routerId,rule.source,rule.prefix,ospfGraph)){const id=\`trackf-redist-bgp:\${rule.id}:\${route.prefix}\`.replaceAll('/','_');if(!bgp.origins.some((entry)=>entry.id===id))bgp.origins.push({id,routerId:rule.routerId,prefix:route.prefix,med:rule.metric,communities:[\`65000:\${rule.routeTag}\`],description:\`REDISTRIBUTED \${rule.source.toUpperCase()} · RULE \${rule.id}\`});if(!bgp.enabledRouterIds.includes(rule.routerId))bgp.enabledRouterIds.push(rule.routerId);}\n  for(const summary of routing.policy.summaries.filter((entry)=>entry.advertiseInto==='bgp'&&summaryActive(graph,addressing,routing,entry,ospfGraph))){const id=\`trackf-summary-bgp:\${summary.id}\`;if(!bgp.origins.some((entry)=>entry.id===id))bgp.origins.push({id,routerId:summary.routerId,prefix:summary.prefix,med:summary.metric,communities:['65000:999'],description:\`SUMMARY \${summary.id}\`});if(!bgp.enabledRouterIds.includes(summary.routerId))bgp.enabledRouterIds.push(summary.routerId);}\n  return bgp;\n}\nfunction summaryDiscardEntries(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,routerId:string,ospfGraph:BuilderGraph):BuilderRouteTableEntry[]{return routing.policy.summaries.filter((entry)=>entry.routerId===routerId&&entry.discard&&summaryActive(graph,addressing,routing,entry,ospfGraph)).map((entry)=>({id:\`summary-discard:\${entry.id}\`,routerId,prefix:entry.prefix,prefixLength:Number(entry.prefix.split('/')[1]),source:'summary',administrativeDistance:254,metric:entry.metric,nextHop:null,outgoingInterface:'Null0',linkId:'discard',active:true,stateNote:\`INTENTIONAL SUMMARY DISCARD · SOURCE \${entry.source.toUpperCase()} · \${entry.description||entry.id}\`,summaryDiscard:true,summarySource:entry.source,summaryId:entry.id}));}\nfunction generalOspfExternalEntries(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,routerId:string):BuilderRouteTableEntry[]{\n  if(!routing.ospf.enabledRouterIds.includes(routerId))return[];const entries:BuilderRouteTableEntry[]=[];for(const rule of routing.policy.redistributions.filter((entry)=>entry.enabled&&entry.target==='ospf')){const areas=builderOspfAreasForRouter(graph,routing.ospf,rule.routerId).filter((area)=>builderOspfAreaType(routing.ospf,area)!=='stub').sort();const areaId=areas[0];if(!areaId)continue;for(const sourceRoute of matchingNativePolicyRoutes(graph,addressing,routing,rule.routerId,rule.source,rule.prefix,graph)){if(rule.routerId===routerId)continue;const paths=builderOspfPathCandidates(graph,routing.ospf,routerId,rule.routerId,areaId);for(const path of paths){if(!path.reachable)continue;for(const firstHop of path.firstHops){const hop=firstHopEntry(graph,addressing,routerId,firstHop.nextRouterId,firstHop.linkId);if(!hop)continue;const nssa=builderOspfAreaType(routing.ospf,areaId)==='nssa'&&path.routeType==='intra-area';entries.push({id:\`ospf-redist-general:\${routerId}:\${rule.id}:\${sourceRoute.prefix}:\${firstHop.linkId}\`.replaceAll('/','_'),routerId,prefix:sourceRoute.prefix,prefixLength:sourceRoute.prefixLength,source:'ospf',administrativeDistance:110,metric:path.totalCost+rule.metric,nextHop:hop.remote.address,outgoingInterface:hop.local.name,linkId:firstHop.linkId,active:true,stateNote:\`\${nssa?'OSPF O N1 · TYPE-7 NSSA':'OSPF O E1 · TYPE-5 EXTERNAL'} · REDISTRIBUTED \${rule.source.toUpperCase()} · TAG \${rule.routeTag}\`,ospfRouteType:nssa?'nssa-external':'external',ospfAreaId:areaId,ospfAbrRouterId:path.destinationAbrRouterId,ospfSummaryId:null,ospfExternalLsaType:nssa?7:5,redistributedFrom:rule.source,redistributionRuleId:rule.id,routeTag:rule.routeTag});}}}}\n  for(const summary of routing.policy.summaries.filter((entry)=>entry.advertiseInto==='ospf'&&summaryActive(graph,addressing,routing,entry,graph))){if(summary.routerId===routerId)continue;const areaId=builderOspfAreasForRouter(graph,routing.ospf,summary.routerId).filter((area)=>builderOspfAreaType(routing.ospf,area)!=='stub').sort()[0];if(!areaId)continue;for(const path of builderOspfPathCandidates(graph,routing.ospf,routerId,summary.routerId,areaId)){for(const firstHop of path.firstHops){const hop=firstHopEntry(graph,addressing,routerId,firstHop.nextRouterId,firstHop.linkId);if(!hop)continue;entries.push({id:\`ospf-summary:\${routerId}:\${summary.id}:\${firstHop.linkId}\`,routerId,prefix:summary.prefix,prefixLength:Number(summary.prefix.split('/')[1]),source:'ospf',administrativeDistance:110,metric:path.totalCost+summary.metric,nextHop:hop.remote.address,outgoingInterface:hop.local.name,linkId:firstHop.linkId,active:true,stateNote:\`OSPF SUMMARY · ORIGIN \${summary.routerId.toUpperCase()} · DISCARD AT ORIGIN\`,ospfRouteType:'external',ospfAreaId:areaId,ospfExternalLsaType:5,redistributedFrom:'summary',summaryId:summary.id});}}}\n  return bestOspfEntries(entries);\n}\nfunction isisInjectedRoutes(graph:BuilderGraph,addressing:BuilderAddressing,routing:BuilderRoutingConfig,ospfGraph:BuilderGraph):BuilderIsisInjectedRoute[]{const result:BuilderIsisInjectedRoute[]=[];for(const rule of routing.policy.redistributions.filter((entry)=>entry.enabled&&entry.target==='isis'))for(const route of matchingNativePolicyRoutes(graph,addressing,routing,rule.routerId,rule.source,rule.prefix,ospfGraph))result.push({id:\`isis-inject:\${rule.id}:\${route.prefix}\`,originRouterId:rule.routerId,prefix:route.prefix,metric:rule.metric,source:rule.source,routeTag:rule.routeTag,redistributionRuleId:rule.id,summaryId:null});for(const summary of routing.policy.summaries.filter((entry)=>entry.advertiseInto==='isis'&&summaryActive(graph,addressing,routing,entry,ospfGraph)))result.push({id:\`isis-summary:\${summary.id}\`,originRouterId:summary.routerId,prefix:summary.prefix,metric:summary.metric,source:'summary',routeTag:999,redistributionRuleId:null,summaryId:summary.id});return result;}\n\n`;
  s=once(s,insertPoint,helpers+insertPoint,'routing policy helpers');
  const oldTable=`export function routeTableForBuilderRouter(graph: BuilderGraph, addressing: BuilderAddressing, routing: BuilderRoutingConfig, routerId: string, ospfTopologyGraph: BuilderGraph = graph): BuilderRouteTableEntry[] {\n  const validated = validateBuilderRoutingConfig(graph, addressing, routing);\n  const baseEntries = base.routeTableForBuilderRouter(graph, addressing, toBaseRouting(validated), routerId, ospfTopologyGraph) as BuilderRouteTableEntry[];\n  return [...baseEntries, ...ospfExternalEntries(ospfTopologyGraph, addressing, validated, routerId), ...ospfStubDefaultEntries(ospfTopologyGraph, addressing, validated, routerId)].sort(compareRoutePreference);\n}`;
  const newTable=`export function routeTableForBuilderRouter(graph: BuilderGraph, addressing: BuilderAddressing, routing: BuilderRoutingConfig, routerId: string, ospfTopologyGraph: BuilderGraph = graph): BuilderRouteTableEntry[] {\n  const validated=validateBuilderRoutingConfig(graph,addressing,routing);const effectiveOspf=builderOspfEffectiveGraph(ospfTopologyGraph,validated.policy,validated.ospf.enabledRouterIds);const bgp=effectiveBgpConfig(graph,addressing,validated,effectiveOspf);const baseRouting={...toBaseRouting(validated),bgp};const baseEntries=base.routeTableForBuilderRouter(graph,addressing,baseRouting,routerId,effectiveOspf) as BuilderRouteTableEntry[];const isis=builderIsisRouteEntriesForRouter(graph,addressing,validated.policy.isis,routerId,isisInjectedRoutes(graph,addressing,validated,effectiveOspf)).map((entry)=>({...entry,source:'isis' as const}));return [...baseEntries,...ospfExternalEntries(effectiveOspf,addressing,validated,routerId),...generalOspfExternalEntries(effectiveOspf,addressing,validated,routerId),...ospfStubDefaultEntries(effectiveOspf,addressing,validated,routerId),...isis,...summaryDiscardEntries(graph,addressing,validated,routerId,effectiveOspf)].sort(compareRoutePreference);\n}`;
  s=once(s,oldTable,newTable,'route table composition');
  s=once(s,"  const candidates = builderEcmpRoutesForDestination(entries, destinationAddress);",`  const candidates = builderEcmpRoutesForDestination(entries, destinationAddress).slice(0,Math.max(1,Math.min(16,arguments.length>3?Number(arguments[3])||16:16)));`,'ecmp max paths');
  const marker="    const selection = selectBuilderRouteWithDecision(routeTableForBuilderRouter(graph, addressing, routing, currentNodeId, ospfTopologyGraph), destinationAddress, flowKey);\n    const selected = selection.route;";
  const replacement=`    const packetKey={sourceAddress:primaryInterfaceForNode(addressing,sourceNodeId)?.address??'0.0.0.0',destinationAddress,protocol:typeof flowKey==='object'&&flowKey?String(flowKey.protocol??'ip'):'ip',sourcePort:typeof flowKey==='object'&&flowKey&&flowKey.sourcePort!=null?flowKey.sourcePort:null,destinationPort:typeof flowKey==='object'&&flowKey&&flowKey.destinationPort!=null?flowKey.destinationPort:null};\n    const ecmp=builderEcmpProfile(routing.policy,currentNodeId);const discriminator=typeof flowKey==='object'&&flowKey?flowKey.discriminator??null:typeof flowKey==='string'?flowKey:null;const hashedKey=builderCanonicalEcmpKey(ecmp.hashMode,packetKey,discriminator);\n    const selection = selectBuilderRouteWithDecision(routeTableForBuilderRouter(graph, addressing, routing, currentNodeId, ospfTopologyGraph), destinationAddress, hashedKey,ecmp.maxPaths);\n    const selected = selection.route;`;
  s=once(s,marker,replacement,'trace ecmp policy');
  const branch=`    let nextNodeId: string | null = null;\n    let nextHop: string | null = selected.nextHop;\n    if (selected.source === 'connected') {`;
  const branchNew=`    let nextNodeId: string | null = null;\n    let nextHop: string | null = selected.nextHop;\n    let actualLinkId=selected.linkId;let actualOutgoingInterface=selected.outgoingInterface;const pbr=builderPbrDecision(routing.policy,currentNodeId,packetKey);\n    if(pbr.matched&&pbr.rule){const owner=interfaceOwner(addressing,pbr.rule.nextHop);const local=owner?addressing.segments[owner.linkId]?.interfaces.find((entry)=>entry.nodeId===currentNodeId):null;if(!owner||!local)return forwardingFailure(sourceNodeId,destinationNodeId,destinationAddress,hops,currentNodeId,'PBR NEXT HOP INVALID');nextNodeId=owner.nodeId;nextHop=pbr.rule.nextHop;actualLinkId=owner.linkId;actualOutgoingInterface=local.name;}\n    else if(selected.source==='summary'&&selected.summaryDiscard)return forwardingFailure(sourceNodeId,destinationNodeId,destinationAddress,hops,currentNodeId,'INTENTIONAL SUMMARY BLACK HOLE');\n    if (!pbr.matched && selected.source === 'connected') {`;
  s=once(s,branch,branchNew,'pbr branch');
  s=once(s,"    } else {\n      const owner = selected.nextHop ? interfaceOwner(addressing, selected.nextHop) : null;",`    } else if(!pbr.matched) {\n      const owner = selected.nextHop ? interfaceOwner(addressing, selected.nextHop) : null;`,'pbr skip normal next hop');
  s=once(s,"    const link = linkById(graph, selected.linkId);\n    if (!link || link.failed) return forwardingFailure(sourceNodeId, destinationNodeId, destinationAddress, hops, currentNodeId, 'OUTGOING LINK DOWN');\n    hops.push({ nodeId: currentNodeId, nodeLabel: current.label, routeSource: selected.source, matchedPrefix: selected.prefix, nextHop, outgoingInterface: selected.outgoingInterface, linkId: selected.linkId, nextNodeId, ecmpCandidateCount: selection.candidates.length, ecmpSelectedIndex: selection.selectedIndex, ecmpFlowHash: selection.flowHash, ecmpFlowKey: selection.flowKey });",`    const link=linkById(graph,actualLinkId);if(!link||link.failed)return forwardingFailure(sourceNodeId,destinationNodeId,destinationAddress,hops,currentNodeId,pbr.matched?'PBR NEXT-HOP LINK DOWN':'OUTGOING LINK DOWN');\n    hops.push({nodeId:currentNodeId,nodeLabel:current.label,routeSource:selected.source,matchedPrefix:selected.prefix,nextHop,outgoingInterface:actualOutgoingInterface,linkId:actualLinkId,nextNodeId,ecmpCandidateCount:selection.candidates.length,ecmpSelectedIndex:selection.selectedIndex,ecmpFlowHash:selection.flowHash,ecmpFlowKey:selection.flowKey,ecmpHashMode:ecmp.hashMode,fibRouteSource:selected.source,fibMatchedPrefix:selected.prefix,fibNextHop:selected.nextHop,pbrRuleId:pbr.rule?.id??null,pbrNextHop:pbr.rule?.nextHop??null});`,'forwarding hop metadata');
  return s;
});

patch('src/builder/bgp.ts',(source)=>{
  let s=source;
  s=once(s,"  allowRelationshipLeak: boolean;\n}",`  allowRelationshipLeak: boolean;\n  routeReflectorClientRouterId: string | null;\n  holdTimeMs: number;\n}`,'bgp session depth');
  s=once(s,"  addCommunity: string | null;\n  allowRelationshipLeak: boolean;",`  addCommunity: string | null;\n  matchCommunity: string | null;\n  removeCommunity: string | null;\n  prependAsCount: number;\n  allowRelationshipLeak: boolean;`,'bgp policy depth');
  s=once(s,"  bestReason: string;\n}",`  bestReason: string;\n  reflectionPath: string[];\n}`,'bgp reflection path');
  s=once(s,"function normalizeCommunities(values: readonly string[]): string[]{return [...new Set(values.map((value)=>String(value).trim()).filter((value)=>/^\\d{1,10}:\\d{1,10}$/.test(value)))].sort();}",`function normalizeCommunities(values: readonly string[]): string[]{return [...new Set(values.map((value)=>String(value).trim().toUpperCase()).filter((value)=>/^\\d{1,10}:\\d{1,10}$/.test(value)||value==='NO_EXPORT'||value==='NO_ADVERTISE'))].sort();}`,'community normalization');
  s=s.replace("allowRelationshipLeak:entry.allowRelationshipLeak===true};","allowRelationshipLeak:entry.allowRelationshipLeak===true,routeReflectorClientRouterId:null,holdTimeMs:Math.max(3000,Math.min(180000,Math.round(Number(entry.holdTimeMs)||90000)))};");
  s=s.replace("return{id,linkId,aRouterId,bRouterId,enabled:entry.enabled!==false,relationship,customerRouterId,nextHopSelf:entry.nextHopSelf===true,allowRelationshipLeak:entry.allowRelationshipLeak===true};","const routeReflectorClientRouterId=relationship==='internal'&&(entry.routeReflectorClientRouterId===aRouterId||entry.routeReflectorClientRouterId===bRouterId)?String(entry.routeReflectorClientRouterId):null;const holdTimeMs=Math.max(3000,Math.min(180000,Math.round(Number(entry.holdTimeMs)||90000));return{id,linkId,aRouterId,bRouterId,enabled:entry.enabled!==false,relationship,customerRouterId,nextHopSelf:entry.nextHopSelf===true,allowRelationshipLeak:entry.allowRelationshipLeak===true,routeReflectorClientRouterId,holdTimeMs};");
  s=once(s,"const community=entry.addCommunity&&/^\\d{1,10}:\\d{1,10}$/.test(String(entry.addCommunity).trim())?String(entry.addCommunity).trim():null;return{id,routerId,direction,sessionId,order,action,prefix,setLocalPref:local,setMed:med,addCommunity:community,allowRelationshipLeak:entry.allowRelationshipLeak===true,description:String(entry.description??'').slice(0,100)};",`const normalizeOne=(value:unknown)=>{const text=String(value??'').trim().toUpperCase();return /^\\d{1,10}:\\d{1,10}$/.test(text)||text==='NO_EXPORT'||text==='NO_ADVERTISE'?text:null;};const community=normalizeOne(entry.addCommunity),matchCommunity=normalizeOne(entry.matchCommunity),removeCommunity=normalizeOne(entry.removeCommunity);const prependAsCount=Math.max(0,Math.min(8,Math.round(Number(entry.prependAsCount)||0)));return{id,routerId,direction,sessionId,order,action,prefix,setLocalPref:local,setMed:med,addCommunity:community,matchCommunity,removeCommunity,prependAsCount,allowRelationshipLeak:entry.allowRelationshipLeak===true,description:String(entry.description??'').slice(0,100)};`,'policy attrs');
  s=once(s,"allowRelationshipLeak:prior?.allowRelationshipLeak??false};",`allowRelationshipLeak:prior?.allowRelationshipLeak??false,routeReflectorClientRouterId:prior?.routeReflectorClientRouterId??null,holdTimeMs:prior?.holdTimeMs??90000};`,'new session defaults');
  s=once(s,"Partial<Pick<BuilderBgpSession,'enabled'|'relationship'|'customerRouterId'|'nextHopSelf'|'allowRelationshipLeak'>>",`Partial<Pick<BuilderBgpSession,'enabled'|'relationship'|'customerRouterId'|'nextHopSelf'|'allowRelationshipLeak'|'routeReflectorClientRouterId'|'holdTimeMs'>>`,'session patch type');
  s=once(s,"function firstPolicy(config:BuilderBgpConfig,routerId:string,direction:BuilderBgpPolicyDirection,sessionId:string,prefix:string):BuilderBgpPolicyRule|null{return config.policies.filter((rule)=>rule.routerId===routerId&&rule.direction===direction&&(!rule.sessionId||rule.sessionId===sessionId)&&prefixContainsPrefix(rule.prefix,prefix)).sort((a,b)=>a.order-b.order||a.id.localeCompare(b.id))[0]??null;}\nfunction mutateAttrs(route:BuilderBgpRoute,rule:BuilderBgpPolicyRule|null):BuilderBgpRoute{if(!rule)return route;return{...route,localPref:rule.setLocalPref??route.localPref,med:rule.setMed??route.med,communities:normalizeCommunities(rule.addCommunity?[...route.communities,rule.addCommunity]:route.communities)};}",`function firstPolicy(config:BuilderBgpConfig,routerId:string,direction:BuilderBgpPolicyDirection,sessionId:string,route:BuilderBgpRoute):BuilderBgpPolicyRule|null{return config.policies.filter((rule)=>rule.routerId===routerId&&rule.direction===direction&&(!rule.sessionId||rule.sessionId===sessionId)&&prefixContainsPrefix(rule.prefix,route.prefix)&&(!rule.matchCommunity||route.communities.includes(rule.matchCommunity))).sort((a,b)=>a.order-b.order||a.id.localeCompare(b.id))[0]??null;}\nfunction mutateAttrs(route:BuilderBgpRoute,rule:BuilderBgpPolicyRule|null):BuilderBgpRoute{if(!rule)return route;let communities=route.communities.filter((value)=>value!==rule.removeCommunity);if(rule.addCommunity)communities=[...communities,rule.addCommunity];return{...route,localPref:rule.setLocalPref??route.localPref,med:rule.setMed??route.med,communities:normalizeCommunities(communities)};}`,'community policy match');
  s=once(s,"route.communities.join(','),route.policyAnomaly].join('|')",`route.communities.join(','),route.policyAnomaly,route.reflectionPath.join(',')].join('|')`,'fingerprint reflection');
  s=s.replace("communities:[...origin.communities],policyAnomaly:false,best:false,bestReason:''};","communities:[...origin.communities],policyAnomaly:false,best:false,bestReason:'',reflectionPath:[]};");
  s=once(s,"asPath:[...route.asPath],communities:[...route.communities]}",`asPath:[...route.asPath],communities:[...route.communities],reflectionPath:[...route.reflectionPath]}`,'clone reflection');
  const oldStart="function advertise(graph:BuilderGraph,addressing:BuilderAddressing,config:BuilderBgpConfig,session:BuilderBgpSession,senderId:string,receiverId:string,input:BuilderBgpRoute):BuilderBgpRoute|null{\n  const mode=sessionMode(graph,config,session);if(mode==='ibgp'&&input.learnedVia==='ibgp')return null;\n  const exportRule=firstPolicy(config,senderId,'export',session.id,input.prefix);if(exportRule?.action==='deny')return null;";
  const newStart="function advertise(graph:BuilderGraph,addressing:BuilderAddressing,config:BuilderBgpConfig,session:BuilderBgpSession,senderId:string,receiverId:string,input:BuilderBgpRoute):BuilderBgpRoute|null{\n  const mode=sessionMode(graph,config,session);if(input.learnedFromRouterId===receiverId)return null;if(input.communities.includes('NO_ADVERTISE'))return null;if(mode==='ebgp'&&input.communities.includes('NO_EXPORT'))return null;if(mode==='ibgp'&&input.learnedVia==='ibgp'){const inbound=config.sessions.find((entry)=>entry.id===input.learnedSessionId);const learnedFromClient=Boolean(inbound&&inbound.routeReflectorClientRouterId===input.learnedFromRouterId&&(inbound.aRouterId===senderId||inbound.bRouterId===senderId));const receiverIsClient=session.routeReflectorClientRouterId===receiverId&&(session.aRouterId===senderId||session.bRouterId===senderId);if(!learnedFromClient&&!receiverIsClient)return null;if(input.reflectionPath.includes(receiverId))return null;}\n  const exportRule=firstPolicy(config,senderId,'export',session.id,input);if(exportRule?.action==='deny')return null;";
  s=once(s,oldStart,newStart,'advertise rr start');
  s=once(s,"let route=mutateAttrs(input,exportRule);",`let route=mutateAttrs(input,exportRule);if(mode==='ebgp'&&exportRule?.prependAsCount){const asn=builderBgpAsnForRouter(graph,config,senderId);route={...route,asPath:[...Array(exportRule.prependAsCount).fill(asn),...route.asPath]};}`,'prepend policy');
  s=once(s,"route={...route,routerId:receiverId,learnedFromRouterId:senderId,learnedSessionId:session.id,learnedVia:'ibgp',nextHopRouterId:nextHopSelf?senderId:route.nextHopRouterId,nextHopAddress,policyAnomaly:anomaly,id:`bgp:${receiverId}:${route.prefix}:${session.id}:${senderId}`};",`route={...route,routerId:receiverId,learnedFromRouterId:senderId,learnedSessionId:session.id,learnedVia:'ibgp',nextHopRouterId:nextHopSelf?senderId:route.nextHopRouterId,nextHopAddress,policyAnomaly:anomaly,reflectionPath:input.learnedVia==='ibgp'?[...new Set([...input.reflectionPath,senderId])]:[...input.reflectionPath],id:\`bgp:\${receiverId}:\${route.prefix}:\${session.id}:\${senderId}\`};`,'rr path');
  s=once(s,"const importRule=firstPolicy(config,receiverId,'import',session.id,route.prefix);",`const importRule=firstPolicy(config,receiverId,'import',session.id,route);`,'import community match');
  s=once(s,"return{graph:{nodes,relationships:[...relMap.values()].sort((a,b)=>a.id.localeCompare(b.id))},sourceAsn,destinationAsn,selectedPathAsns,prefix:winner?.prefix??(prefix?parsePrefix(prefix).cidr:null),selectedRoute};\n}",`return{graph:{nodes,relationships:[...relMap.values()].sort((a,b)=>a.id.localeCompare(b.id))},sourceAsn,destinationAsn,selectedPathAsns,prefix:winner?.prefix??(prefix?parsePrefix(prefix).cidr:null),selectedRoute};\n}\n\nexport interface BuilderBgpWithdrawalEvent { id:string; atMs:number; kind:'LINK_DOWN'|'HOLD_TIMER_EXPIRED'|'ROUTE_WITHDRAWN'|'BEST_PATH_RECOMPUTED'; summary:string }\nexport interface BuilderBgpWithdrawalScenario { sessionId:string; holdTimeMs:number; beforeState:BuilderBgpState; afterState:BuilderBgpState; events:BuilderBgpWithdrawalEvent[] }\nexport function createBuilderBgpWithdrawalScenario(graph:BuilderGraph,addressing:BuilderAddressing,config:BuilderBgpConfig,sessionId:string):BuilderBgpWithdrawalScenario{const validated=validateBuilderBgpConfig(graph,config);const session=validated.sessions.find((entry)=>entry.id===sessionId);if(!session)throw new Error(\`Unknown BGP session \${sessionId}.\`);const beforeState=builderBgpState(graph,addressing,validated);const after=cloneBuilderBgpConfig(validated);after.sessions=after.sessions.map((entry)=>entry.id===sessionId?{...entry,enabled:false}:entry);const afterState=builderBgpState(graph,addressing,after);const holdTimeMs=session.holdTimeMs;return{sessionId,holdTimeMs,beforeState,afterState,events:[{id:'link-down',atMs:0,kind:'LINK_DOWN',summary:'Peering transport is lost; previously learned routes remain stale until the configured hold timer expires.'},{id:'hold-expired',atMs:holdTimeMs,kind:'HOLD_TIMER_EXPIRED',summary:\`BGP hold timer \${(holdTimeMs/1000).toFixed(0)}s expired.\`},{id:'withdraw',atMs:holdTimeMs,kind:'ROUTE_WITHDRAWN',summary:'Routes learned only through the failed session are withdrawn from Adj-RIB-In/RIB.'},{id:'best',atMs:holdTimeMs,kind:'BEST_PATH_RECOMPUTED',summary:'The same deterministic BGP best-path engine recomputes from remaining advertisements.'}]};}\nexport function snapshotBuilderBgpWithdrawal(scenario:BuilderBgpWithdrawalScenario,elapsedMs:number):{elapsedMs:number;stale:boolean;state:BuilderBgpState;visibleEvents:BuilderBgpWithdrawalEvent[]}{const elapsed=Math.max(0,Math.round(elapsedMs));const stale=elapsed<scenario.holdTimeMs;return{elapsedMs:elapsed,stale,state:stale?scenario.beforeState:scenario.afterState,visibleEvents:scenario.events.filter((event)=>event.atMs<=elapsed)};}\n}`,'withdrawal model');
  return s;
});

patch('src/BuilderOspfTimingPanel.tsx',(source)=>{
  let s=source;
  s=once(s,"import { createBuilderOspfLinkFailureScenario, DEFAULT_BUILDER_OSPF_TIMING, snapshotBuilderOspfConvergence } from './builder/ospf-timing.ts';",`import { createBuilderOspfLinkFailureScenario, snapshotBuilderOspfConvergence } from './builder/ospf-timing.ts';\nimport { builderOspfTimerFor } from './builder/routing-policy.ts';`,'timing imports');
  s=once(s,"    try { return createBuilderOspfLinkFailureScenario(graph, addressing, routing, sourceId, destinationId, selectedLink.id); }",`    try { const a=builderOspfTimerFor(routing.policy,selectedLink.a,selectedLink.id),b=builderOspfTimerFor(routing.policy,selectedLink.b,selectedLink.id);const timer=a.helloIntervalMs===b.helloIntervalMs&&a.deadIntervalMs===b.deadIntervalMs?a:{...a,helloIntervalMs:Math.min(a.helloIntervalMs,b.helloIntervalMs),deadIntervalMs:Math.min(a.deadIntervalMs,b.deadIntervalMs)};return createBuilderOspfLinkFailureScenario(graph,addressing,routing,sourceId,destinationId,selectedLink.id,{helloIntervalMs:timer.helloIntervalMs,deadIntervalMs:timer.deadIntervalMs,lsaFloodMs:200,spfDelayMs:500,ribInstallMs:100,fibInstallMs:100}); }`,'configured timer scenario');
  s=once(s,"<small className=\"builder-routing-note\">TIMED TEACHING MODEL · HELLO {DEFAULT_BUILDER_OSPF_TIMING.helloIntervalMs/1000}s · DEAD {DEFAULT_BUILDER_OSPF_TIMING.deadIntervalMs/1000}s · PHYSICAL FAILURE, NEIGHBOR KNOWLEDGE, SPF, RIB, FIB, AND TRAFFIC RECOVERY STAY DISTINCT.</small>",`<small className=\"builder-routing-note\">TIMED TEACHING MODEL · HELLO {(scenario.profile.helloIntervalMs/1000).toFixed(1)}s · DEAD {(scenario.profile.deadIntervalMs/1000).toFixed(1)}s · CONFIGURED INTERFACE TIMERS DRIVE FAILURE DETECTION; PHYSICAL FAILURE, NEIGHBOR KNOWLEDGE, SPF, RIB, FIB, AND TRAFFIC RECOVERY STAY DISTINCT.</small>`,'timing label');
  return s;
});

patch('src/BuilderBgpPanel.tsx',(source)=>{
  let s=source;
  s=once(s,"  type BuilderBgpAsProjection,\n} from './builder/bgp.ts';",`  type BuilderBgpAsProjection,\n  createBuilderBgpWithdrawalScenario, snapshotBuilderBgpWithdrawal,\n} from './builder/bgp.ts';`,'bgp panel timing imports');
  s=once(s,"const [allowLeak,setAllowLeak]=useState(false);",`const [allowLeak,setAllowLeak]=useState(false);const [matchCommunity,setMatchCommunity]=useState('');const [removeCommunity,setRemoveCommunity]=useState('');const [prependAsCount,setPrependAsCount]=useState(0);const [withdrawMs,setWithdrawMs]=useState(0);`,'bgp panel state');
  s=once(s,"addCommunity:policyCommunity.trim()||null,allowRelationshipLeak:allowLeak",`addCommunity:policyCommunity.trim()||null,matchCommunity:matchCommunity.trim()||null,removeCommunity:removeCommunity.trim()||null,prependAsCount,allowRelationshipLeak:allowLeak`,'bgp panel policy attrs');
  s=once(s,"<button type=\"button\" onClick={()=>patchSession({nextHopSelf:!selectedSession.nextHopSelf},`iBGP NEXT-HOP-SELF · ${!selectedSession.nextHopSelf?'enabled':'disabled'}.`)}>NEXT-HOP-SELF {selectedSession.nextHopSelf?'ON':'OFF'}</button>",`<button type=\"button\" onClick={()=>patchSession({nextHopSelf:!selectedSession.nextHopSelf},\`iBGP NEXT-HOP-SELF · \${!selectedSession.nextHopSelf?'enabled':'disabled'}.\`)}>NEXT-HOP-SELF {selectedSession.nextHopSelf?'ON':'OFF'}</button><button type=\"button\" onClick={()=>patchSession({routeReflectorClientRouterId:selectedSession.routeReflectorClientRouterId?null:(selectedSession.aRouterId===selectedNodeId?selectedSession.bRouterId:selectedSession.aRouterId)},'iBGP ROUTE REFLECTOR CLIENT role updated.')}>RR CLIENT {selectedSession.routeReflectorClientRouterId?labelFor(graph,selectedSession.routeReflectorClientRouterId):'OFF'}</button>`,'rr ui');
  s=once(s,"<label>ADD COMMUNITY<input value={policyCommunity} onChange={(event)=>setPolicyCommunity(event.currentTarget.value)} placeholder=\"NONE\"/></label><label><input type=\"checkbox\"",`<label>ADD COMMUNITY<input value={policyCommunity} onChange={(event)=>setPolicyCommunity(event.currentTarget.value)} placeholder=\"65000:100 / NO_EXPORT\"/></label><label>MATCH COMMUNITY<input value={matchCommunity} onChange={(event)=>setMatchCommunity(event.currentTarget.value)} placeholder=\"ANY\"/></label><label>REMOVE COMMUNITY<input value={removeCommunity} onChange={(event)=>setRemoveCommunity(event.currentTarget.value)} placeholder=\"NONE\"/></label><label>AS PREPEND<input type=\"number\" min={0} max={8} value={prependAsCount} onChange={(event)=>setPrependAsCount(Number(event.currentTarget.value))}/></label><label><input type=\"checkbox\"`,'community ui');
  s=once(s,"COMMUNITY {rule.addCommunity??'KEEP'}{rule.allowRelationshipLeak?' · LEAK OVERRIDE':''}",`COMMUNITY +{rule.addCommunity??'KEEP'} / MATCH {rule.matchCommunity??'ANY'} / REMOVE {rule.removeCommunity??'NONE'} · PREPEND {rule.prependAsCount}{rule.allowRelationshipLeak?' · LEAK OVERRIDE':''}`,'policy display');
  s=once(s,"<div className=\"control-title\"><span>BGP RIB</span><strong>{bestRoutes.length} BEST · {selectedRoutes.length} CANDIDATES</strong></div>",`{selectedSession&&sessionState?.state==='ESTABLISHED'&&(()=>{let scenario;try{scenario=createBuilderBgpWithdrawalScenario(graph,addressing,bgp,selectedSession.id);}catch{return null;}const snap=snapshotBuilderBgpWithdrawal(scenario,withdrawMs);return <><div className=\"control-title\"><span>WITHDRAWAL TIMING</span><strong>{snap.stale?'STALE ROUTES RETAINED':'WITHDRAWN + RECOMPUTED'}</strong></div><label>HOLD TIMER<input type=\"number\" min={3000} max={180000} step={1000} value={selectedSession.holdTimeMs} onChange={(event)=>patchSession({holdTimeMs:Number(event.currentTarget.value)},'BGP HOLD TIMER updated.')}/></label><input type=\"range\" min={0} max={scenario.holdTimeMs+10000} step={1000} value={Math.min(withdrawMs,scenario.holdTimeMs+10000)} onChange={(event)=>setWithdrawMs(Number(event.currentTarget.value))}/><small className=\"builder-routing-note\">t={(snap.elapsedMs/1000).toFixed(0)}s · {snap.visibleEvents.map((event)=>event.kind.replaceAll('_',' ')).join(' → ')||'LINK UP'} · SAME BGP ENGINE, TIMED STALE-RIB PROJECTION.</small></>;})()}<div className=\"control-title\"><span>BGP RIB</span><strong>{bestRoutes.length} BEST · {selectedRoutes.length} CANDIDATES</strong></div>`,'withdrawal ui');
  return s;
});

patch('src/NetworkBuilder.tsx',(source)=>{
  let s=source;
  s=once(s,"import { BuilderOspfTimingPanel } from './BuilderOspfTimingPanel.tsx';\nimport { BuilderOspfEcmpPanel } from './BuilderOspfEcmpPanel.tsx';\nimport { BuilderOspfAreaPanel } from './BuilderOspfAreaPanel.tsx';\nimport { BuilderIpv6Panel } from './BuilderIpv6Panel.tsx';\nimport { BuilderBgpPanel } from './BuilderBgpPanel.tsx';",`import { BuilderIpv6Panel } from './BuilderIpv6Panel.tsx';`,'remove static depth panels');
  s=once(s,"const BuilderAuthoringPanel = lazy(() => import('./BuilderAuthoringPanel.tsx'));",`const BuilderAuthoringPanel = lazy(() => import('./BuilderAuthoringPanel.tsx'));\nconst BuilderOspfTimingPanel=lazy(()=>import('./BuilderOspfTimingPanel.tsx').then((m)=>({default:m.BuilderOspfTimingPanel})));\nconst BuilderOspfEcmpPanel=lazy(()=>import('./BuilderOspfEcmpPanel.tsx').then((m)=>({default:m.BuilderOspfEcmpPanel})));\nconst BuilderOspfAreaPanel=lazy(()=>import('./BuilderOspfAreaPanel.tsx').then((m)=>({default:m.BuilderOspfAreaPanel})));\nconst BuilderBgpPanel=lazy(()=>import('./BuilderBgpPanel.tsx').then((m)=>({default:m.BuilderBgpPanel})));\nconst BuilderRoutingPolicyPanel=lazy(()=>import('./BuilderRoutingPolicyPanel.tsx'));`,'lazy depth panels');
  s=once(s,"          <BuilderOspfAreaPanel graph={graph} addressing={addressing} routing={routing} selectedNodeId={selectedNodeId} selectedLinkId={selectedLinkId} onChange={(next, detail)=>{setRouting(next);setMessage(detail);}}/>\n          <BuilderOspfEcmpPanel graph={graph} addressing={addressing} routing={routing} sourceId={sourceId} destinationId={destinationId}/>\n          <BuilderOspfTimingPanel graph={graph} addressing={addressing} routing={routing} sourceId={sourceId} destinationId={destinationId}/>\n          {!stressLabel&&<BuilderBgpPanel graph={graph} addressing={addressing} routing={routing} selectedNodeId={selectedNodeId} selectedLinkId={selectedLinkId} destinationPrefix={destinationPrefix} onChange={setRouting} onMessage={setMessage} onOpenAsProjection={openBgpProjection}/>} ",`          {!stressLabel&&<Suspense fallback={null}><BuilderOspfAreaPanel graph={graph} addressing={addressing} routing={routing} selectedNodeId={selectedNodeId} selectedLinkId={selectedLinkId} onChange={(next, detail)=>{setRouting(next);setMessage(detail);}}/><BuilderOspfEcmpPanel graph={graph} addressing={addressing} routing={routing} sourceId={sourceId} destinationId={destinationId}/><BuilderOspfTimingPanel graph={graph} addressing={addressing} routing={routing} sourceId={sourceId} destinationId={destinationId}/><BuilderBgpPanel graph={graph} addressing={addressing} routing={routing} selectedNodeId={selectedNodeId} selectedLinkId={selectedLinkId} destinationPrefix={destinationPrefix} onChange={setRouting} onMessage={setMessage} onOpenAsProjection={openBgpProjection}/><BuilderRoutingPolicyPanel graph={graph} addressing={addressing} routing={routing} selectedNodeId={selectedNodeId} selectedLinkId={selectedLinkId} sourceId={sourceId} destinationId={destinationId} historical={isHistorical} onChange={setRouting} onMessage={setMessage}/></Suspense>}`,'lazy panel render');
  s=s.replace("entry.source==='bgp'?(entry.bgpLearnedVia==='ebgp'?'B':'B i'):entry.ospfRouteType==='inter-area'?'O IA':'O'","entry.source==='bgp'?(entry.bgpLearnedVia==='ebgp'?'B':'B i'):entry.source==='isis'?'i':entry.source==='summary'?'Σ':entry.ospfRouteType==='inter-area'?'O IA':entry.ospfRouteType==='external'?'O E1':entry.ospfRouteType==='nssa-external'?'O N1':'O'");
  s=s.replace("LOOKUP: LONGEST PREFIX → AD → OSPF ROUTE TYPE (O BEFORE O IA) → METRIC → ECMP FLOW HASH. CONNECTED AD 0 · STATIC AD 1 · eBGP AD 20 · OSPF AD 110 · iBGP AD 200.","LOOKUP: LONGEST PREFIX → AD → PROTOCOL ROUTE TYPE → METRIC → BOUNDED ECMP HASH. PBR MAY OVERRIDE THE RESOLVED NEXT HOP WHILE THE DESTINATION FIB DECISION REMAINS VISIBLE. CONNECTED 0 · STATIC 1 · eBGP 20 · OSPF 110 · IS-IS 115 · iBGP 200 · SUMMARY DISCARD 254.");
  s=s.replace("{hop.routeSource.toUpperCase()} · {hop.matchedPrefix ?? '—'} · {hop.nextHop ?? 'LOCAL'} · {hop.outgoingInterface ?? '—'}","{hop.routeSource.toUpperCase()} · {hop.matchedPrefix ?? '—'} · {hop.nextHop ?? 'LOCAL'} · {hop.outgoingInterface ?? '—'}{hop.pbrRuleId?` · PBR ${hop.pbrRuleId} · FIB ${hop.fibMatchedPrefix}`:''}{hop.ecmpCandidateCount&&hop.ecmpCandidateCount>1?` · ECMP ${hop.ecmpSelectedIndex!+1}/${hop.ecmpCandidateCount} ${hop.ecmpHashMode?.toUpperCase()}`:''}");
  return s;
});

patch('package.json',(source)=>{const data=JSON.parse(source);data.scripts['test:builder-routing-policy-contract']='node scripts/builder-routing-policy-contract-check.mjs';data.scripts.check=data.scripts.check.replace('npm run test:builder-data-plane-contract','npm run test:builder-data-plane-contract && npm run test:builder-routing-policy-contract');return JSON.stringify(data,null,2)+'\n';});

unlinkSync('scripts/track-f-patch.mjs');
unlinkSync('.github/workflows/track-f-patch.yml');
