import fs from 'node:fs';

function patch(path, transform) {
  const before = fs.readFileSync(path, 'utf8');
  const after = transform(before);
  if (after === before) throw new Error(`No changes produced for ${path}`);
  fs.writeFileSync(path, after);
}

function replaceOne(text, from, to, label) {
  const index = text.indexOf(from);
  if (index < 0) throw new Error(`Missing patch anchor: ${label}`);
  if (text.indexOf(from, index + from.length) >= 0) throw new Error(`Patch anchor is not unique: ${label}`);
  return text.slice(0, index) + to + text.slice(index + from.length);
}

patch('src/builder/challenges.ts', (input) => {
  let text = input;
  text = replaceOne(text,
    "import { validateBuilderAddressing, type BuilderAddressing } from './addressing.ts';",
    "import { interfacesForBuilderNode, validateBuilderAddressing, type BuilderAddressing } from './addressing.ts';\nimport { upsertBuilderAclRule, type BuilderAclConfig, type BuilderAclRule } from './acl.ts';",
    'challenge addressing/ACL imports');
  text = replaceOne(text,
    "import { validateBuilderEthernetConfig, type BuilderEthernetConfig } from './ethernet.ts';",
    "import { validateBuilderEthernetConfig, type BuilderEthernetConfig } from './ethernet.ts';\nimport { validateBuilderNatConfig, type BuilderNatConfig } from './nat.ts';",
    'challenge NAT import');
  text = replaceOne(text,
    "export type BuilderChallengeEvidenceKind = 'ping' | 'traceroute' | 'ethernet-flow' | 'arp-resolution' | 'inspect-config' | 'inspect-state' | 'inspect-events';\nexport type BuilderChallengeFamily = 'gateway' | 'access-vlan' | 'trunk-vlan' | 'stp-loop' | 'static-route' | 'ospf-disabled';",
    "export type BuilderChallengeEvidenceKind = 'ping' | 'traceroute' | 'ethernet-flow' | 'arp-resolution' | 'nat-flow' | 'inspect-config' | 'inspect-state' | 'inspect-events';\nexport type BuilderChallengeFamily = 'gateway' | 'access-vlan' | 'trunk-vlan' | 'stp-loop' | 'static-route' | 'ospf-disabled' | 'acl-deny' | 'nat-disabled';",
    'challenge kind/family unions');
  text = replaceOne(text,
    "export interface BuilderOspfDisabledChallengeFault {\n  kind: 'ospf-router-disabled';\n  boundary: 'ROUTING';\n  plane: 'routed';\n  nodeId: string;\n  expectedEnabled: true;\n}\n\nexport type BuilderChallengeFault = BuilderGatewayChallengeFault | BuilderAccessVlanChallengeFault | BuilderTrunkVlanChallengeFault | BuilderStpChallengeFault | BuilderStaticRouteChallengeFault | BuilderOspfDisabledChallengeFault;\n\nexport interface BuilderChallengeVerification {\n  kind: 'routed-probe' | 'ethernet-flow';",
    "export interface BuilderOspfDisabledChallengeFault {\n  kind: 'ospf-router-disabled';\n  boundary: 'ROUTING';\n  plane: 'routed';\n  nodeId: string;\n  expectedEnabled: true;\n}\n\nexport interface BuilderAclDenyChallengeFault {\n  kind: 'acl-objective-deny';\n  boundary: 'POLICY';\n  plane: 'routed';\n  nodeId: string;\n  blockingRule: BuilderAclRule;\n}\n\nexport interface BuilderNatDisabledChallengeFault {\n  kind: 'nat-boundary-disabled';\n  boundary: 'POLICY';\n  plane: 'routed';\n  nodeId: string;\n  boundaryId: string;\n  expectedEnabled: true;\n}\n\nexport type BuilderChallengeFault = BuilderGatewayChallengeFault | BuilderAccessVlanChallengeFault | BuilderTrunkVlanChallengeFault | BuilderStpChallengeFault | BuilderStaticRouteChallengeFault | BuilderOspfDisabledChallengeFault | BuilderAclDenyChallengeFault | BuilderNatDisabledChallengeFault;\n\nexport interface BuilderChallengeVerification {\n  kind: 'routed-probe' | 'ethernet-flow' | 'nat-translation';",
    'policy fault interfaces');
  text = replaceOne(text,
    "export function createBuilderChallenge(seedInput: string): BuilderChallenge {",
    `export function createAclDenyChallenge(seedInput: string): BuilderChallenge {\n  const seed = normalizeSeed(seedInput);\n  const hash = hashSeed(seed);\n  const healthy = defaultHealthySnapshot();\n  healthy.sourceId = 'client';\n  healthy.destinationId = 'app';\n  const sourceAddress = interfacesForBuilderNode(healthy.addressing, 'client')[0]?.address;\n  const destinationAddress = interfacesForBuilderNode(healthy.addressing, 'app')[0]?.address;\n  if (!sourceAddress || !destinationAddress) throw new Error('The ACL challenge requires canonical CLIENT and APP IPv4 addresses.');\n  const candidates = ['edge', 'core'].filter((id) => healthy.graph.nodes.some((node) => node.id === id && node.kind === 'router'));\n  if (candidates.length !== 2) throw new Error('The ACL challenge requires canonical EDGE and CORE routers.');\n  const nodeId = candidates[hash % candidates.length];\n  const blockingRule: BuilderAclRule = {\n    id: \\`challenge-acl-\\${hash.toString(16).padStart(8, '0')}\\`, routerId: nodeId, order: 5, action: 'deny', protocol: 'icmp',\n    sourcePrefix: \\`\\${sourceAddress}/32\\`, destinationPrefix: \\`\\${destinationAddress}/32\\`, destinationPort: null, description: 'Track J objective ICMP deny',\n  };\n  const broken = createBuilderAuthoringSnapshot(healthy);\n  broken.acl = upsertBuilderAclRule(broken.graph, broken.acl, blockingRule);\n  return {\n    schema: BUILDER_CHALLENGE_SCHEMA, version: BUILDER_CHALLENGE_VERSION, id: \\`acl-\\${hash.toString(16).padStart(8, '0')}\\`, seed, family: 'acl-deny',\n    title: 'ICMP POLICY BLOCKS THE PATH',\n    objective: 'Restore IPv4 diagnostic reachability from CLIENT to APP. Routing remains healthy; use ordinary Ping / Traceroute plus policy state and Device Workbench to identify and remove the blocking canonical ACL rule.',\n    difficulty: 'FOUNDATION', healthy, broken, verification: { kind: 'routed-probe', sourceId: 'client', destinationId: 'app' },\n    fault: { kind: 'acl-objective-deny', boundary: 'POLICY', plane: 'routed', nodeId, blockingRule },\n  };\n}\n\nexport function createNatDisabledChallenge(seedInput: string): BuilderChallenge {\n  const seed = normalizeSeed(seedInput);\n  const hash = hashSeed(seed);\n  const healthy = defaultHealthySnapshot();\n  healthy.sourceId = 'client';\n  healthy.destinationId = 'app';\n  const boundary = healthy.nat.boundaries.find((entry) => entry.routerId === 'edge');\n  if (!boundary || !boundary.enabled) throw new Error('The NAT challenge requires the canonical enabled EDGE NAT boundary.');\n  const broken = createBuilderAuthoringSnapshot(healthy);\n  broken.nat = validateBuilderNatConfig(broken.graph, {\n    ...broken.nat,\n    boundaries: broken.nat.boundaries.map((entry) => entry.id === boundary.id ? { ...entry, enabled: false } : entry),\n  });\n  return {\n    schema: BUILDER_CHALLENGE_SCHEMA, version: BUILDER_CHALLENGE_VERSION, id: \\`nat-\\${hash.toString(16).padStart(8, '0')}\\`, seed, family: 'nat-disabled',\n    title: 'PAT BOUNDARY GOES DARK',\n    objective: 'Restore required PAT translation from CLIENT to APP. The routed flow may still deliver untranslated; use the ordinary NAT outbound tool and Device Workbench to prove whether the canonical EDGE boundary is actually translating.',\n    difficulty: 'FOUNDATION', healthy, broken, verification: { kind: 'nat-translation', sourceId: 'client', destinationId: 'app' },\n    fault: { kind: 'nat-boundary-disabled', boundary: 'POLICY', plane: 'routed', nodeId: boundary.routerId, boundaryId: boundary.id, expectedEnabled: true },\n  };\n}\n\nexport function createBuilderChallenge(seedInput: string): BuilderChallenge {`,
    'policy challenge generators');
  text = replaceOne(text,
    "  if (lowered.startsWith('ospf-')) return createOspfDisabledChallenge(seed);\n  return createDefaultGatewayChallenge(seed);",
    "  if (lowered.startsWith('ospf-')) return createOspfDisabledChallenge(seed);\n  if (lowered.startsWith('acl-') || lowered.startsWith('firewall-')) return createAclDenyChallenge(seed);\n  if (lowered.startsWith('nat-') || lowered.startsWith('pat-')) return createNatDisabledChallenge(seed);\n  return createDefaultGatewayChallenge(seed);",
    'policy challenge dispatch');
  text = replaceOne(text,
    "export function builderChallengeIsRepaired(challenge: BuilderChallenge, addressing: BuilderAddressing, ethernet: BuilderEthernetConfig, routing: BuilderRoutingConfig): boolean {",
    "export function builderChallengeIsRepaired(challenge: BuilderChallenge, addressing: BuilderAddressing, ethernet: BuilderEthernetConfig, routing: BuilderRoutingConfig, acl: BuilderAclConfig = challenge.broken.acl, nat: BuilderNatConfig = challenge.broken.nat): boolean {",
    'repair signature');
  text = replaceOne(text,
    "  if (fault.kind === 'missing-static-route') {\n    return routing.staticRoutes.some((route) => route.id === fault.expectedRoute.id && route.routerId === fault.expectedRoute.routerId && route.prefix === fault.expectedRoute.prefix && route.nextHop === fault.expectedRoute.nextHop && route.metric === fault.expectedRoute.metric);\n  }\n  return routing.ospf.enabledRouterIds.includes(fault.nodeId) === fault.expectedEnabled;",
    "  if (fault.kind === 'missing-static-route') {\n    return routing.staticRoutes.some((route) => route.id === fault.expectedRoute.id && route.routerId === fault.expectedRoute.routerId && route.prefix === fault.expectedRoute.prefix && route.nextHop === fault.expectedRoute.nextHop && route.metric === fault.expectedRoute.metric);\n  }\n  if (fault.kind === 'ospf-router-disabled') return routing.ospf.enabledRouterIds.includes(fault.nodeId) === fault.expectedEnabled;\n  if (fault.kind === 'acl-objective-deny') return !acl.rules.some((rule) => rule.id === fault.blockingRule.id);\n  const boundary = nat.boundaries.find((entry) => entry.id === fault.boundaryId && entry.routerId === fault.nodeId);\n  return boundary?.enabled === fault.expectedEnabled;",
    'policy repair predicates');
  text = replaceOne(text,
    "  if (fault.kind === 'missing-static-route') return `${fault.nodeId.toUpperCase()} was missing static route ${fault.expectedRoute.prefix} via ${fault.expectedRoute.nextHop}. Restoring that canonical route repaired forwarding and the post-repair routed probe verified it.`;\n  return `${fault.nodeId.toUpperCase()} was not participating in OSPF. Re-enabling the canonical OSPF process restored route learning and the post-repair routed probe verified reachability.`;",
    "  if (fault.kind === 'missing-static-route') return `${fault.nodeId.toUpperCase()} was missing static route ${fault.expectedRoute.prefix} via ${fault.expectedRoute.nextHop}. Restoring that canonical route repaired forwarding and the post-repair routed probe verified it.`;\n  if (fault.kind === 'ospf-router-disabled') return `${fault.nodeId.toUpperCase()} was not participating in OSPF. Re-enabling the canonical OSPF process restored route learning and the post-repair routed probe verified reachability.`;\n  if (fault.kind === 'acl-objective-deny') return `${fault.nodeId.toUpperCase()} had an explicit ICMP deny for the challenge source/destination. Removing the canonical blocking rule restored policy permission and the post-repair probe verified reachability.`;\n  return `${fault.nodeId.toUpperCase()} had the canonical NAT boundary disabled. Re-enabling it restored PAT translation; the post-repair NAT flow proved the tuple was translated rather than merely routed.`;",
    'policy solved explanations');
  text = replaceOne(text,
    "  routing: BuilderRoutingConfig,\n): BuilderChallengeScore {",
    "  routing: BuilderRoutingConfig,\n  acl: BuilderAclConfig = challenge.broken.acl,\n  nat: BuilderNatConfig = challenge.broken.nat,\n): BuilderChallengeScore {",
    'score signature');
  text = replaceOne(text,
    "  if (challenge.verification.kind === 'routed-probe') {\n    const failedPing = hasEvidence(evidence, (entry) => entry.kind === 'ping' && isObjectiveEvidence(challenge, entry) && entry.success === false && !entry.repaired);\n    const failedTraceroute = hasEvidence(evidence, (entry) => entry.kind === 'traceroute' && isObjectiveEvidence(challenge, entry) && entry.success === false && !entry.repaired);\n    evidenceScore = (failedPing ? 10 : 0) + (failedTraceroute ? 10 : 0) + (inspectedState ? 10 : 0) + (inspectedConfig ? 10 : 0);\n    hasPrimaryDiagnostic = failedPing || failedTraceroute;\n  } else {\n    const failedLanFlow = hasEvidence(evidence, (entry) => entry.kind === 'ethernet-flow' && isObjectiveEvidence(challenge, entry) && entry.success === false && !entry.repaired);\n    const observedArp = hasEvidence(evidence, (entry) => entry.kind === 'arp-resolution' && isObjectiveEvidence(challenge, entry) && !entry.repaired);\n    evidenceScore = (failedLanFlow ? 15 : 0) + (observedArp ? 5 : 0) + (inspectedState ? 10 : 0) + (inspectedConfig ? 10 : 0);\n    hasPrimaryDiagnostic = failedLanFlow;\n  }",
    "  if (challenge.verification.kind === 'routed-probe') {\n    const failedPing = hasEvidence(evidence, (entry) => entry.kind === 'ping' && isObjectiveEvidence(challenge, entry) && entry.success === false && !entry.repaired);\n    const failedTraceroute = hasEvidence(evidence, (entry) => entry.kind === 'traceroute' && isObjectiveEvidence(challenge, entry) && entry.success === false && !entry.repaired);\n    evidenceScore = (failedPing ? 10 : 0) + (failedTraceroute ? 10 : 0) + (inspectedState ? 10 : 0) + (inspectedConfig ? 10 : 0);\n    hasPrimaryDiagnostic = failedPing || failedTraceroute;\n  } else if (challenge.verification.kind === 'ethernet-flow') {\n    const failedLanFlow = hasEvidence(evidence, (entry) => entry.kind === 'ethernet-flow' && isObjectiveEvidence(challenge, entry) && entry.success === false && !entry.repaired);\n    const observedArp = hasEvidence(evidence, (entry) => entry.kind === 'arp-resolution' && isObjectiveEvidence(challenge, entry) && !entry.repaired);\n    evidenceScore = (failedLanFlow ? 15 : 0) + (observedArp ? 5 : 0) + (inspectedState ? 10 : 0) + (inspectedConfig ? 10 : 0);\n    hasPrimaryDiagnostic = failedLanFlow;\n  } else {\n    const missingTranslation = hasEvidence(evidence, (entry) => entry.kind === 'nat-flow' && isObjectiveEvidence(challenge, entry) && entry.success === false && !entry.repaired);\n    evidenceScore = (missingTranslation ? 20 : 0) + (inspectedState ? 10 : 0) + (inspectedConfig ? 10 : 0);\n    hasPrimaryDiagnostic = missingTranslation;\n  }",
    'NAT evidence scoring');
  text = replaceOne(text,
    "  const repaired = builderChallengeIsRepaired(challenge, addressing, ethernet, routing);\n  const verified = challenge.verification.kind === 'routed-probe'\n    ? hasEvidence(evidence, (entry) => (entry.kind === 'ping' || entry.kind === 'traceroute') && isObjectiveEvidence(challenge, entry) && entry.success === true && entry.repaired)\n    : hasEvidence(evidence, (entry) => entry.kind === 'ethernet-flow' && isObjectiveEvidence(challenge, entry) && entry.success === true && entry.repaired);",
    "  const repaired = builderChallengeIsRepaired(challenge, addressing, ethernet, routing, acl, nat);\n  const verified = challenge.verification.kind === 'routed-probe'\n    ? hasEvidence(evidence, (entry) => (entry.kind === 'ping' || entry.kind === 'traceroute') && isObjectiveEvidence(challenge, entry) && entry.success === true && entry.repaired)\n    : challenge.verification.kind === 'ethernet-flow'\n      ? hasEvidence(evidence, (entry) => entry.kind === 'ethernet-flow' && isObjectiveEvidence(challenge, entry) && entry.success === true && entry.repaired)\n      : hasEvidence(evidence, (entry) => entry.kind === 'nat-flow' && isObjectiveEvidence(challenge, entry) && entry.success === true && entry.repaired);",
    'NAT verification');
  return text;
});

patch('src/BuilderChallengePanel.tsx', (input) => {
  let text = input;
  text = replaceOne(text,
    "  if (entry.kind === 'arp-resolution') return 'ARP';\n  if (entry.kind === 'inspect-config') return 'INSPECT CONFIG';",
    "  if (entry.kind === 'arp-resolution') return 'ARP';\n  if (entry.kind === 'nat-flow') return 'NAT FLOW';\n  if (entry.kind === 'inspect-config') return 'INSPECT CONFIG';",
    'NAT evidence label');
  text = replaceOne(text,
    "  const routedChallenge = challenge.verification.kind === 'routed-probe';",
    "  const verificationKind = challenge.verification.kind;",
    'challenge verification kind');
  text = replaceOne(text,
    "      <p>{routedChallenge\n        ? 'Run the ordinary Builder Ping / Traceroute tools and inspect CONFIG / STATE / EVENTS in Device Workbench. Repair the network with the normal Builder configuration controls, then prove the repair with another objective probe.'\n        : 'Run the ordinary LAN SEND FRAME / PACKET flow, inspect its ARP result, and use CONFIG / STATE / EVENTS in Device Workbench. Repair the normal VLAN / trunk / STP controls, then rerun the exact LAN objective to prove the fix.'}</p>",
    "      <p>{verificationKind === 'routed-probe'\n        ? 'Run the ordinary Builder Ping / Traceroute tools and inspect CONFIG / STATE / EVENTS in Device Workbench. Repair the network with the normal Builder configuration controls, then prove the repair with another objective probe.'\n        : verificationKind === 'ethernet-flow'\n          ? 'Run the ordinary LAN SEND FRAME / PACKET flow, inspect its ARP result, and use CONFIG / STATE / EVENTS in Device Workbench. Repair the normal VLAN / trunk / STP controls, then rerun the exact LAN objective to prove the fix.'\n          : 'Run the ordinary NAT RUN OUTBOUND tool and inspect CONFIG / STATE / EVENTS in Device Workbench. A delivered but untranslated tuple is still a failed objective. Repair the canonical NAT boundary, then rerun the same outbound flow to prove PAT translation.'}</p>",
    'NAT challenge instructions');
  return text;
});

patch('src/BuilderNatPanel.tsx', (input) => {
  let text = input;
  text = replaceOne(text,
    "  onMessage: (message: string) => void;\n}",
    "  onMessage: (message: string) => void;\n  onFlowResult?: (result: BuilderNatFlowResult, sourceId: string, destinationId: string) => void;\n}",
    'NAT result callback prop');
  text = replaceOne(text,
    "export function BuilderNatPanel({ graph, addressing, routing, acl, nat, onNatChange, sessions, onSessionsChange, sourceId, destinationId, onMessage }: BuilderNatPanelProps) {",
    "export function BuilderNatPanel({ graph, addressing, routing, acl, nat, onNatChange, sessions, onSessionsChange, sourceId, destinationId, onMessage, onFlowResult }: BuilderNatPanelProps) {",
    'NAT result callback destructure');
  text = replaceOne(text,
    "      onSessionsChange(result.sessions); setLastResult(result); setSequence((value) => value + 1);\n      onMessage(`NAT ${result.success ? 'FLOW' : 'FAILED'} · ${result.explanation}`);",
    "      onSessionsChange(result.sessions); setLastResult(result); setSequence((value) => value + 1); onFlowResult?.(result, sourceId, destinationId);\n      onMessage(`NAT ${result.success ? 'FLOW' : 'FAILED'} · ${result.explanation}`);",
    'outbound NAT result callback');
  text = replaceOne(text,
    "      onSessionsChange(result.sessions); setLastResult(result); setSequence((value) => value + 1);\n      onMessage(`NAT RETURN ${result.success ? 'MATCHED' : 'FAILED'} · ${result.explanation}`);",
    "      onSessionsChange(result.sessions); setLastResult(result); setSequence((value) => value + 1); onFlowResult?.(result, destinationId, sourceId);\n      onMessage(`NAT RETURN ${result.success ? 'MATCHED' : 'FAILED'} · ${result.explanation}`);",
    'inbound NAT result callback');
  return text;
});

patch('src/NetworkBuilder.tsx', (input) => {
  let text = input;
  text = replaceOne(text,
    "const challengeScore = useMemo(() => challenge ? scoreBuilderChallenge(challenge, challengeEvidence, challengeHypothesis, addressing, ethernet, routing) : null, [challenge, challengeEvidence, challengeHypothesis, addressing, ethernet, routing]);",
    "const challengeScore = useMemo(() => challenge ? scoreBuilderChallenge(challenge, challengeEvidence, challengeHypothesis, addressing, ethernet, routing, acl, nat) : null, [challenge, challengeEvidence, challengeHypothesis, addressing, ethernet, routing, acl, nat]);",
    'live policy challenge score');
  text = text.replaceAll('builderChallengeIsRepaired(challenge,addressing,ethernet,routing)', 'builderChallengeIsRepaired(challenge,addressing,ethernet,routing,acl,nat)');
  text = replaceOne(text,
    "else{setSourceId(next.verification.sourceId);setDestinationId(next.verification.destinationId);setWorkbenchDevice({plane:'routed',id:next.fault.nodeId});setSelectedNodeId(next.fault.nodeId);}",
    "else{setSourceId(next.verification.sourceId);setDestinationId(next.verification.destinationId);setWorkbenchDevice({plane:'routed',id:next.verification.sourceId});setSelectedNodeId(next.verification.sourceId);}",
    'challenge start source focus');
  text = replaceOne(text,
    "else{setSourceId(challenge.verification.sourceId);setDestinationId(challenge.verification.destinationId);setWorkbenchDevice({plane:'routed',id:challenge.fault.nodeId});setSelectedNodeId(challenge.fault.nodeId);}",
    "else{setSourceId(challenge.verification.sourceId);setDestinationId(challenge.verification.destinationId);setWorkbenchDevice({plane:'routed',id:challenge.verification.sourceId});setSelectedNodeId(challenge.verification.sourceId);}",
    'challenge restart source focus');
  text = replaceOne(text,
    "<button type=\"button\" onClick={()=>setChallengeSeed('ospf-001')}>OSPF</button></div><button type=\"button\" disabled={isHistorical} onClick={()=>startChallenge(challengeSeed)}>START CHALLENGE</button><small className=\"builder-routing-note\">SEED PREFIX SELECTS FAMILY · GATEWAY / VLAN / TRUNK / STP / STATIC / OSPF · NORMAL BUILDER PROBES, LAN FLOW, ARP, ROUTE/OSPF STATE, WORKBENCH, AND CONFIG CONTROLS · SESSION-ONLY SCORE.</small>",
    "<button type=\"button\" onClick={()=>setChallengeSeed('ospf-001')}>OSPF</button><button type=\"button\" onClick={()=>setChallengeSeed('acl-001')}>ACL</button><button type=\"button\" onClick={()=>setChallengeSeed('nat-001')}>NAT</button></div><button type=\"button\" disabled={isHistorical} onClick={()=>startChallenge(challengeSeed)}>START CHALLENGE</button><small className=\"builder-routing-note\">SEED PREFIX SELECTS FAMILY · GATEWAY / VLAN / TRUNK / STP / STATIC / OSPF / ACL / NAT · NORMAL BUILDER PROBES, LAN FLOW, NAT FLOW, POLICY/ROUTE STATE, WORKBENCH, AND CONFIG CONTROLS · SESSION-ONLY SCORE.</small>",
    'policy challenge launcher');
  text = replaceOne(text,
    "<BuilderNatPanel graph={graph} addressing={addressing} routing={routing} acl={acl} nat={nat} onNatChange={setNat} sessions={natSessions} onSessionsChange={setNatSessions} sourceId={sourceId} destinationId={destinationId} onMessage={setMessage}/>",
    "<BuilderNatPanel graph={graph} addressing={addressing} routing={routing} acl={acl} nat={nat} onNatChange={setNat} sessions={natSessions} onSessionsChange={setNatSessions} sourceId={sourceId} destinationId={destinationId} onMessage={setMessage} onFlowResult={(result,flowSourceId,flowDestinationId)=>{if(!challenge||challenge.verification.kind!=='nat-translation'||isHistorical||result.direction!=='outbound')return;const repaired=builderChallengeIsRepaired(challenge,addressing,ethernet,routing,acl,nat);const translated=Boolean(result.translation);setChallengeEvidence((current)=>appendBuilderChallengeEvidence(current,{kind:'nat-flow',sourceId:flowSourceId,destinationId:flowDestinationId,success:translated,repaired,detail:translated?`PAT OBSERVED · ${result.explanation}`:`NO TRANSLATION · ${result.explanation}`}));}}/>",
    'NAT challenge evidence integration');
  return text;
});

patch('scripts/builder-challenge-contract-check.mjs', (input) => {
  let text = input;
  text = replaceOne(text,
    "  createAccessVlanChallenge,\n  createBuilderChallenge,",
    "  createAccessVlanChallenge,\n  createAclDenyChallenge,\n  createBuilderChallenge,",
    'ACL contract import');
  text = replaceOne(text,
    "  createMissingStaticRouteChallenge,\n  createOspfDisabledChallenge,",
    "  createMissingStaticRouteChallenge,\n  createNatDisabledChallenge,\n  createOspfDisabledChallenge,",
    'NAT contract import');
  text = replaceOne(text,
    "import { runBuilderProbe } from '../src/builder/probes.ts';",
    "import { runBuilderNatOutboundFlow, validateBuilderNatConfig } from '../src/builder/nat.ts';\nimport { runBuilderProbe } from '../src/builder/probes.ts';",
    'NAT contract helpers');
  const insertion = `\n\nconst aclChallenge = createAclDenyChallenge('acl-contract-001');\nassert.equal(aclChallenge.family, 'acl-deny');\nassert.equal(aclChallenge.fault.kind, 'acl-objective-deny');\nassert.deepEqual(aclChallenge, createBuilderChallenge('acl-contract-001'));\nassert.equal(runPing(aclChallenge.healthy).success, true, 'ACL healthy baseline must pass ordinary Ping');\nassert.equal(runPing(aclChallenge.broken).success, false, 'ACL deny must fail ordinary Ping without challenge-only forwarding');\nconst repairedAcl = structuredClone(aclChallenge.broken.acl);\nrepairedAcl.rules = repairedAcl.rules.filter((rule) => rule.id !== aclChallenge.fault.blockingRule.id);\nassert.deepEqual(repairedAcl, aclChallenge.healthy.acl, 'ACL challenge adds exactly one canonical blocking rule');\nassert.equal(builderChallengeIsRepaired(aclChallenge, aclChallenge.broken.addressing, aclChallenge.broken.ethernet, aclChallenge.broken.routing, aclChallenge.broken.acl, aclChallenge.broken.nat), false);\nassert.equal(builderChallengeIsRepaired(aclChallenge, aclChallenge.broken.addressing, aclChallenge.broken.ethernet, aclChallenge.broken.routing, repairedAcl, aclChallenge.broken.nat), true);\nlet aclEvidence = [];\naclEvidence = appendBuilderChallengeEvidence(aclEvidence,{kind:'ping',sourceId:'client',destinationId:'app',success:false,repaired:false,detail:'Objective Ping denied by policy.'});\naclEvidence = appendBuilderChallengeEvidence(aclEvidence,{kind:'traceroute',sourceId:'client',destinationId:'app',success:false,repaired:false,detail:'Objective Traceroute denied by policy.'});\naclEvidence = recordInspection(aclEvidence,aclChallenge,'state');\naclEvidence = recordInspection(aclEvidence,aclChallenge,'config');\nconst aclHypothesis={boundary:'POLICY',deviceId:aclChallenge.fault.nodeId};\nassert.deepEqual(scoreBuilderChallenge(aclChallenge,aclEvidence,aclHypothesis,aclChallenge.broken.addressing,aclChallenge.broken.ethernet,aclChallenge.broken.routing,aclChallenge.broken.acl,aclChallenge.broken.nat),{evidence:40,reasoning:20,repair:0,verification:0,total:60,repaired:false,verified:false,solved:false});\nassert.equal(scoreBuilderChallenge(aclChallenge,aclEvidence,aclHypothesis,aclChallenge.broken.addressing,aclChallenge.broken.ethernet,aclChallenge.broken.routing,repairedAcl,aclChallenge.broken.nat).total,85);\naclEvidence=appendBuilderChallengeEvidence(aclEvidence,{kind:'ping',sourceId:'client',destinationId:'app',success:true,repaired:true,detail:'Objective Ping passes after ACL repair.'});\nassert.equal(scoreBuilderChallenge(aclChallenge,aclEvidence,aclHypothesis,aclChallenge.broken.addressing,aclChallenge.broken.ethernet,aclChallenge.broken.routing,repairedAcl,aclChallenge.broken.nat).total,100);\n\nfunction runNatObjective(snapshot){return runBuilderNatOutboundFlow(snapshot.graph,snapshot.addressing,snapshot.routing,snapshot.nat,[],'client','app','tcp',51515,443,1,snapshot.acl);}\nconst natChallenge=createNatDisabledChallenge('nat-contract-001');\nassert.equal(natChallenge.family,'nat-disabled');\nassert.equal(natChallenge.fault.kind,'nat-boundary-disabled');\nassert.deepEqual(natChallenge,createBuilderChallenge('nat-contract-001'));\nconst healthyNatFlow=runNatObjective(natChallenge.healthy);\nconst brokenNatFlow=runNatObjective(natChallenge.broken);\nassert.equal(healthyNatFlow.success,true);\nassert.ok(healthyNatFlow.translation,'healthy NAT objective must produce canonical PAT translation');\nassert.equal(brokenNatFlow.success,true,'disabling NAT does not invent a routing outage');\nassert.equal(brokenNatFlow.translation,null,'broken NAT objective must expose untranslated delivery');\nconst repairedNat=validateBuilderNatConfig(natChallenge.broken.graph,{...natChallenge.broken.nat,boundaries:natChallenge.broken.nat.boundaries.map((entry)=>entry.id===natChallenge.fault.boundaryId?{...entry,enabled:true}:entry)});\nassert.deepEqual(repairedNat,natChallenge.healthy.nat,'NAT challenge changes exactly the canonical boundary enabled state');\nlet natEvidence=[];\nnatEvidence=appendBuilderChallengeEvidence(natEvidence,{kind:'nat-flow',sourceId:'client',destinationId:'app',success:false,repaired:false,detail:brokenNatFlow.explanation});\nnatEvidence=recordInspection(natEvidence,natChallenge,'state');\nnatEvidence=recordInspection(natEvidence,natChallenge,'config');\nconst natHypothesis={boundary:'POLICY',deviceId:natChallenge.fault.nodeId};\nassert.deepEqual(scoreBuilderChallenge(natChallenge,natEvidence,natHypothesis,natChallenge.broken.addressing,natChallenge.broken.ethernet,natChallenge.broken.routing,natChallenge.broken.acl,natChallenge.broken.nat),{evidence:40,reasoning:20,repair:0,verification:0,total:60,repaired:false,verified:false,solved:false});\nassert.equal(scoreBuilderChallenge(natChallenge,natEvidence,natHypothesis,natChallenge.broken.addressing,natChallenge.broken.ethernet,natChallenge.broken.routing,natChallenge.broken.acl,repairedNat).total,85);\nnatEvidence=appendBuilderChallengeEvidence(natEvidence,{kind:'nat-flow',sourceId:'app',destinationId:'client',success:true,repaired:true,detail:'Unrelated NAT observation.'});\nassert.equal(scoreBuilderChallenge(natChallenge,natEvidence,natHypothesis,natChallenge.broken.addressing,natChallenge.broken.ethernet,natChallenge.broken.routing,natChallenge.broken.acl,repairedNat).verified,false,'unrelated NAT flow cannot verify the objective');\nconst repairedNatFlow=runNatObjective({...natChallenge.broken,nat:repairedNat});\nassert.ok(repairedNatFlow.translation);\nnatEvidence=appendBuilderChallengeEvidence(natEvidence,{kind:'nat-flow',sourceId:'client',destinationId:'app',success:true,repaired:true,detail:repairedNatFlow.explanation});\nassert.deepEqual(scoreBuilderChallenge(natChallenge,natEvidence,natHypothesis,natChallenge.broken.addressing,natChallenge.broken.ethernet,natChallenge.broken.routing,natChallenge.broken.acl,repairedNat),{evidence:40,reasoning:20,repair:25,verification:15,total:100,repaired:true,verified:true,solved:true});\n`;
  text = replaceOne(text,
    "\nfor (const challenge of [access, trunk, stp, staticRoute, ospf]) {",
    `${insertion}\nfor (const challenge of [access, trunk, stp, staticRoute, ospf, aclChallenge, natChallenge]) {`,
    'policy contract tests');
  text = replaceOne(text,
    "console.log('Builder Track J challenge contract passed: gateway plus seeded access-VLAN, trunk-pruning, STP-loop, missing-static-route, and OSPF-disabled faults use canonical truth, ordinary probes/LAN+ARP evidence, exact repair, objective-scoped verification, causal scoring, and reproducible tokens.');",
    "console.log('Builder Track J challenge contract passed: gateway plus seeded access-VLAN, trunk-pruning, STP-loop, missing-static-route, OSPF-disabled, ACL-deny, and NAT-disabled faults use canonical truth, ordinary probes/LAN+ARP/NAT evidence, exact repair, objective-scoped verification, causal scoring, and reproducible tokens.');",
    'policy contract summary');
  return text;
});

patch('docs/TRACKJ.md', (input) => input + `\n\n## Fourth slice — ACL and NAT policy\n\nTrack J now includes two deterministic policy families:\n\n- \`acl-*\` / \`firewall-*\`: the healthy routed baseline gains one objective-specific ICMP deny on a deterministic EDGE/CORE router. Routing remains healthy; ordinary Ping / Traceroute and Device Workbench expose the policy failure. Repair uses the existing ACL delete control.\n- \`nat-*\` / \`pat-*\`: the canonical EDGE NAT boundary is disabled. The underlying routed flow may still deliver untranslated, so the objective is explicitly PAT correctness rather than a fabricated reachability outage. The existing NAT RUN OUTBOUND surface produces structured challenge evidence; repair uses the normal ENABLE NAT control and verification requires an actual translated tuple.\n\nThe scoring contract remains 40 evidence + 20 causal reasoning + 25 canonical repair + 15 objective verification. NAT evidence awards the 20 diagnostic-flow points when the objective outbound flow is observed without translation, plus normal target STATE and CONFIG inspection.\n\nChallenge scoring now consumes live ACL and NAT configuration in addition to addressing, Ethernet, and routing truth. Evidence and scores remain session-only and never influence ACL evaluation, NAT translation, forwarding, or provenance.\n`);

console.log('Applied Track J ACL/NAT policy challenge slice.');
